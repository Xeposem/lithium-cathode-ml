---
phase: 07-fix-pipeline-orchestrator-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cathode_ml/pipeline.py
  - cathode_ml/models/baselines.py
  - tests/test_pipeline.py
  - tests/test_baselines.py
autonomous: true
requirements: [EVAL-01, EVAL-02, EVAL-03, DATA-04]

must_haves:
  truths:
    - "pipeline.py loads features.yaml, baselines.yaml, cgcnn.yaml, megnet.yaml separately (not from data.yaml)"
    - "pipeline.py reads records from data/processed/materials.json (not DataCache)"
    - "baselines.py saves results to data/results/baselines/baseline_results.json (matching load_all_results expectation)"
    - "load_all_results finds baseline results and includes RF+XGBoost in unified output"
  artifacts:
    - path: "cathode_ml/pipeline.py"
      provides: "Fixed run_train_stage with separate config loading and JSON data reading"
      contains: "load_config.*features\\.yaml"
    - path: "cathode_ml/models/baselines.py"
      provides: "Fixed baseline results save path"
      contains: "baselines.*baseline_results\\.json"
    - path: "tests/test_pipeline.py"
      provides: "TestRunTrainStage class verifying config and data loading"
      contains: "TestRunTrainStage"
    - path: "tests/test_baselines.py"
      provides: "Test verifying baseline results saved to baselines/ subdirectory"
      contains: "baselines.*baseline_results"
  key_links:
    - from: "cathode_ml/pipeline.py"
      to: "configs/features.yaml"
      via: "load_config call"
      pattern: "load_config.*features\\.yaml"
    - from: "cathode_ml/pipeline.py"
      to: "data/processed/materials.json"
      via: "json.load + MaterialRecord"
      pattern: "materials\\.json"
    - from: "cathode_ml/models/baselines.py"
      to: "cathode_ml/evaluation/metrics.py"
      via: "consistent results path"
      pattern: "baselines.*baseline_results\\.json"
---

<objective>
Fix the three cross-phase wiring bugs in the pipeline orchestrator so that `python -m cathode_ml` correctly loads config files, reads processed data, and produces baseline results findable by the evaluation loader.

Purpose: The standalone CLIs (train_cgcnn, train_megnet, baselines) work correctly, but pipeline.py uses incorrect config loading (monolithic data.yaml) and data reading (DataCache) patterns. baselines.py saves results to the wrong path. These bugs prevent end-to-end pipeline execution and unified evaluation.

Output: Fixed pipeline.py and baselines.py, with new tests proving the wiring is correct.
</objective>

<execution_context>
@C:/Users/regis/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/regis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-fix-pipeline-orchestrator-wiring/07-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From cathode_ml/pipeline.py (lines 92-135 -- the broken run_train_stage):
```python
def run_train_stage(args: argparse.Namespace) -> None:
    # Currently loads from data.yaml and DataCache -- MUST be fixed
    from cathode_ml.config import load_config
    from cathode_ml.data.cache import DataCache
    config = load_config(str(Path(args.config_dir) / "data.yaml"))
    cache = DataCache(cache_dir="data/cache")
    records = cache.load("cleaned_records")
    features_config = config.get("features", {})
```

From cathode_ml/models/baselines.py (line 196 -- wrong save path):
```python
results_path = str(Path(results_dir) / "baseline_results.json")
# Should be: str(Path(results_dir) / "baselines" / "baseline_results.json")
```

From cathode_ml/evaluation/metrics.py (line 62 -- expected load path):
```python
baselines_path = base / "baselines" / "baseline_results.json"
```

From cathode_ml/config.py:
```python
def load_config(path: str) -> dict: ...
```

From cathode_ml/data/schemas.py:
```python
@dataclass
class MaterialRecord:
    material_id: str
    formula: str
    structure_dict: dict
    source: str
    formation_energy_per_atom: float | None = None
    energy_above_hull: float | None = None
    voltage: float | None = None
    capacity: float | None = None
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write tests for config loading, data reading, and baseline save path</name>
  <files>tests/test_pipeline.py, tests/test_baselines.py</files>
  <behavior>
    - Test: run_train_stage calls load_config with "features.yaml", "baselines.yaml", "cgcnn.yaml", "megnet.yaml" (not "data.yaml")
    - Test: run_train_stage opens and reads "data/processed/materials.json" (not DataCache.load)
    - Test: run_train_stage constructs MaterialRecord objects from JSON records
    - Test: run_baselines saves results JSON to {results_dir}/baselines/baseline_results.json (not {results_dir}/baseline_results.json)
  </behavior>
  <action>
Add a `TestRunTrainStage` class to `tests/test_pipeline.py` with these tests:

1. `test_loads_separate_config_files` -- Create a mock args namespace with `config_dir="configs"`, `models=["rf", "xgb", "cgcnn", "megnet"]`, `seed=42`. Patch `cathode_ml.pipeline.load_config` (it's imported inside the function, so patch `cathode_ml.config.load_config`), patch `builtins.open` for the JSON file read, patch `json.load` to return a list of fake record dicts, and patch each model trainer (`run_baselines`, `train_cgcnn`, `train_megnet`). Assert that `load_config` was called with paths ending in `features.yaml`, `baselines.yaml`, `cgcnn.yaml`, `megnet.yaml`. Assert it was NOT called with a path ending in `data.yaml`.

2. `test_loads_processed_records` -- Same setup but focus on data loading. Patch `builtins.open` (use `mock_open`) and `json.load` to return `[{"material_id": "test-1", "formula": "LiCoO2", "structure_dict": {}, "source": "test"}]`. Verify `open` was called with a `Path` object whose string contains `data/processed/materials.json`. Verify the records list passed to trainers contains `MaterialRecord` instances.

Note: Because `run_train_stage` uses lazy imports (`from cathode_ml.config import load_config` inside the function body), patching must target the module where it's defined: `cathode_ml.config.load_config`. Use `patch("cathode_ml.config.load_config")` and also patch the data loading. Since multiple patches interact, the simplest approach is to use `unittest.mock.patch` as decorators/context managers and verify call args.

Add a test to `tests/test_baselines.py`:

3. `test_results_saved_to_baselines_subdir` -- Run `run_baselines` with `tmp_path` fixtures (reuse existing `fake_records`, `baselines_config`, `features_cfg` fixtures). After running, assert that `{tmp_path}/results/baselines/baseline_results.json` exists and is valid JSON. Assert that `{tmp_path}/results/baseline_results.json` does NOT exist (the old wrong path).

Run tests -- they MUST FAIL (RED phase) since the fixes are not applied yet.
  </action>
  <verify>
    <automated>python -m pytest tests/test_pipeline.py::TestRunTrainStage tests/test_baselines.py::test_results_saved_to_baselines_subdir -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>New test classes exist and fail with clear error messages indicating the wiring bugs (wrong config path, wrong data source, wrong save path)</done>
</task>

<task type="auto">
  <name>Task 2: Fix pipeline.py config loading, data reading, and baselines.py save path</name>
  <files>cathode_ml/pipeline.py, cathode_ml/models/baselines.py</files>
  <action>
**Fix 1: pipeline.py run_train_stage config loading and data reading (lines 92-135)**

Replace the entire `run_train_stage` function body. The function signature stays the same.

Change imports inside the function:
- Remove: `from cathode_ml.data.cache import DataCache`
- Add: `import json` and `from cathode_ml.data.schemas import MaterialRecord`
- Keep: `from cathode_ml.config import load_config`

Change config loading:
- Remove: `config = load_config(str(Path(args.config_dir) / "data.yaml"))`
- Remove: `features_config = config.get("features", {})`
- Add: `config_dir = Path(args.config_dir)`
- Add: `features_config = load_config(str(config_dir / "features.yaml"))`

Change data loading:
- Remove: `cache = DataCache(cache_dir="data/cache")` and `records = cache.load("cleaned_records")`
- Add: `processed_path = Path("data/processed/materials.json")`
- Add: `with open(processed_path) as f:` / `raw_records = json.load(f)`
- Add: `records = [MaterialRecord(**r) for r in raw_records]`

Change per-model config loading:
- Baselines block: Replace `config.get("baselines", {})` with `load_config(str(config_dir / "baselines.yaml"))`
- CGCNN block: Replace `config.get("cgcnn", {})` with `load_config(str(config_dir / "cgcnn.yaml"))`
- MEGNet block: Replace `config.get("megnet", {})` with `load_config(str(config_dir / "megnet.yaml"))`

**Fix 2: baselines.py results save path (line 196)**

Change line 196 from:
```python
results_path = str(Path(results_dir) / "baseline_results.json")
```
To:
```python
results_path = str(Path(results_dir) / "baselines" / "baseline_results.json")
```

The `baselines_dir = Path(results_dir) / "baselines"` is already created with `mkdir(parents=True, exist_ok=True)` on line 134-135, so the parent directory already exists.

No other files need changes. The evaluation loader (`metrics.py`) already expects `baselines/baseline_results.json`.
  </action>
  <verify>
    <automated>python -m pytest tests/test_pipeline.py tests/test_baselines.py tests/test_evaluation.py -x -q</automated>
  </verify>
  <done>All tests pass. run_train_stage loads 4 separate YAML configs and reads from data/processed/materials.json. baselines.py saves to data/results/baselines/baseline_results.json matching metrics.py load path.</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_pipeline.py tests/test_baselines.py tests/test_evaluation.py -x -q` -- all pass
2. `python -m pytest tests/ -x -q` -- full suite green (no regressions)
3. `grep -n "data\.yaml" cathode_ml/pipeline.py` -- returns no matches (old pattern removed)
4. `grep -n "DataCache" cathode_ml/pipeline.py` -- returns no matches (old pattern removed)
5. `grep -n "baselines.*baseline_results" cathode_ml/models/baselines.py` -- confirms new path
</verification>

<success_criteria>
- pipeline.py loads features.yaml, baselines.yaml, cgcnn.yaml, megnet.yaml separately
- pipeline.py reads from data/processed/materials.json using json.load + MaterialRecord
- baselines.py saves results to data/results/baselines/baseline_results.json
- All existing tests still pass (no regressions)
- New tests verify the specific wiring fixes
</success_criteria>

<output>
After completion, create `.planning/phases/07-fix-pipeline-orchestrator-wiring/07-01-SUMMARY.md`
</output>
