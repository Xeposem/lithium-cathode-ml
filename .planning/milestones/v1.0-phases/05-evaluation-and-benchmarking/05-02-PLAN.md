---
phase: 05-evaluation-and-benchmarking
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - cathode_ml/evaluation/plots.py
  - cathode_ml/evaluation/__main__.py
  - tests/test_plots.py
autonomous: true
requirements:
  - EVAL-03
  - EVAL-04
  - EVAL-05

must_haves:
  truths:
    - "Parity plots (2x2 subplot per property, 4 figures total) are saved as 300 DPI PNGs"
    - "Bar chart comparison shows all models side-by-side across properties"
    - "Learning curves plot train loss and val loss per epoch for CGCNN and MEGNet"
    - "All figures use Wong colorblind-safe palette and Nature minimal style"
    - "Running python -m cathode_ml.evaluation generates all figures and tables"
  artifacts:
    - path: "cathode_ml/evaluation/plots.py"
      provides: "All matplotlib figure generation functions"
      exports: ["apply_nature_style", "plot_parity", "plot_bar_comparison", "plot_learning_curves"]
    - path: "cathode_ml/evaluation/__main__.py"
      provides: "CLI entry point for evaluation subcommand"
    - path: "tests/test_plots.py"
      provides: "Unit tests for plot generation"
  key_links:
    - from: "cathode_ml/evaluation/plots.py"
      to: "cathode_ml/evaluation/metrics.py"
      via: "import load_all_results, MODEL_COLORS, MODEL_LABELS"
      pattern: "from cathode_ml\\.evaluation\\.metrics import"
    - from: "cathode_ml/evaluation/plots.py"
      to: "data/results/cgcnn/*_metrics.csv"
      via: "pd.read_csv for learning curves"
      pattern: "pd\\.read_csv.*metrics\\.csv"
    - from: "cathode_ml/evaluation/plots.py"
      to: "data/results/figures/"
      via: "fig.savefig output directory"
      pattern: "savefig.*figures"
---

<objective>
Create the plotting module that generates all publication-quality figures (parity plots, bar charts, learning curves) and the evaluation CLI entry point.

Purpose: Visualization of model comparison results with Nature/Science journal aesthetics.
Output: plots.py with all figure generators, __main__.py CLI, tests proving figure generation.
</objective>

<execution_context>
@C:/Users/regis/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/regis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-evaluation-and-benchmarking/05-CONTEXT.md
@.planning/phases/05-evaluation-and-benchmarking/05-RESEARCH.md
@.planning/phases/05-evaluation-and-benchmarking/05-01-SUMMARY.md

@cathode_ml/evaluation/metrics.py
@cathode_ml/models/utils.py

<interfaces>
<!-- From Plan 01 (metrics.py) -->
```python
MODEL_COLORS = {"rf": "#0072B2", "xgb": "#D55E00", "cgcnn": "#009E73", "megnet": "#CC79A7"}
MODEL_LABELS = {"rf": "RF", "xgb": "XGBoost", "cgcnn": "CGCNN", "megnet": "MEGNet\u2020"}
MODELS_ORDER = ["rf", "xgb", "cgcnn", "megnet"]
PROPERTIES = ["formation_energy_per_atom", "voltage", "capacity", "energy_above_hull"]

def load_all_results(results_base: str = "data/results") -> dict:
    # Returns: {property: {model: {mae, rmse, r2, n_train, n_test}}}

def generate_all_tables(results_base: str = "data/results") -> None:
    # Writes comparison.md and comparison.json
```

CSV per-epoch format (CGCNN): epoch,train_loss,val_loss,val_mae,lr
CSV per-epoch format (MEGNet): epoch,train_loss,val_loss,val_mae,train_mae (lr may be missing)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create plots module with Nature-style parity plots, bar charts, and learning curves</name>
  <files>cathode_ml/evaluation/plots.py, tests/test_plots.py</files>
  <behavior>
    - test_apply_nature_style: After calling apply_nature_style(), rcParams match NATURE_STYLE dict (no grid, spines off, 300 DPI)
    - test_plot_parity_creates_file: plot_parity creates a PNG file at the specified path
    - test_plot_parity_2x2_layout: Generated figure has 2x2 subplot grid
    - test_plot_parity_annotations: Each panel has R-squared and MAE text annotations
    - test_plot_bar_comparison_creates_file: plot_bar_comparison creates a PNG file
    - test_plot_bar_comparison_handles_missing: Missing model for a property does not crash
    - test_plot_learning_curves_creates_file: plot_learning_curves creates a PNG from mock CSV data
    - test_plot_learning_curves_handles_missing_csv: Missing CSV files result in "No data" panels, not crashes
    - test_plot_learning_curves_handles_missing_lr: MEGNet CSV without lr column plots successfully
  </behavior>
  <action>
    Create `cathode_ml/evaluation/plots.py` with:

    1. `NATURE_STYLE` dict (per research -- locked decision):
       - font.family: sans-serif, font.sans-serif: ["Arial", "Helvetica", "DejaVu Sans"]
       - font.size: 8, axes.titlesize: 9, axes.labelsize: 8, tick sizes: 7, legend: 7
       - axes.linewidth: 0.8, axes.spines.top: False, axes.spines.right: False
       - axes.grid: False, figure.dpi: 300, savefig.dpi: 300
       - savefig.bbox: "tight", savefig.pad_inches: 0.05

    2. `apply_nature_style()`: Updates matplotlib.rcParams with NATURE_STYLE

    3. `plot_parity(property_name: str, model_predictions: dict, output_path: str) -> None`:
       - 2x2 subplot grid, figsize=(6, 6) (locked decision: one per property, 4 panels)
       - model_predictions: {model_key: {"y_true": array, "y_pred": array, "mae": float, "r2": float}}
       - Iterate MODELS_ORDER for panel assignment (rf top-left, xgb top-right, cgcnn bottom-left, megnet bottom-right)
       - Each panel: scatter with MODEL_COLORS, size=10, alpha=0.6, no edge
       - Diagonal reference line (k--, 0.8 width, 0.5 alpha)
       - Panel title from MODEL_LABELS
       - Upper-left text annotation: "R^2 = {:.3f}\nMAE = {:.3f}" (locked decision)
       - Axes: "Actual" x, "Predicted" y
       - suptitle with property name (title-cased, underscores to spaces)
       - plt.close(fig) after savefig (no plt.show -- anti-pattern from research)
       - Handle missing model by setting panel invisible

    4. `plot_bar_comparison(all_results: dict, output_path: str) -> None`:
       - Grouped bar chart: one group per property, one bar per model
       - Use MAE as the comparison metric (most interpretable)
       - Wong palette colors per model
       - Legend with MODEL_LABELS, frameon=False
       - x-axis: property names (title-cased), y-axis: "MAE"
       - Handle missing models gracefully (skip bar, adjust positions)

    5. `plot_learning_curves(results_base: str, output_path: str) -> None`:
       - Read CSV files: {results_base}/{model}/{property}_metrics.csv
       - Grid: rows=properties, cols=2 (CGCNN, MEGNet)
       - Plot train_loss (dark gray #333333) and val_loss (model color) per panel
       - Handle missing CSV files: show "No data" text
       - Handle missing columns (lr for MEGNet): skip, do not crash
       - Use pandas read_csv with error handling

    Create `tests/test_plots.py`:
    - Use matplotlib.use("Agg") backend for headless testing
    - Create mock model_predictions dicts with synthetic data
    - Create mock CSV files in tmp_path for learning curve tests
    - Verify file creation, do NOT verify pixel-level image content
    - Use `matplotlib.pyplot.close("all")` in teardown to prevent memory leaks
  </action>
  <verify>
    <automated>cd D:/GitRepo/lithium-cathode-ml && python -m pytest tests/test_plots.py -x -v</automated>
  </verify>
  <done>
    - All three plot types (parity, bar, learning curves) generate PNG files
    - Nature style applied (no grid, clean spines, 300 DPI)
    - Wong colorblind-safe palette used for all model colors
    - MEGNet labeled with dagger annotation
    - Missing data handled gracefully in all plot types
  </done>
</task>

<task type="auto">
  <name>Task 2: Create evaluation CLI entry point</name>
  <files>cathode_ml/evaluation/__main__.py</files>
  <action>
    Create `cathode_ml/evaluation/__main__.py`:

    1. Import argparse, logging
    2. Build parser with:
       - `--results-dir`: Path to results directory (default: "data/results")
       - `--output-dir`: Path for figures output (default: "data/results/figures")
    3. Main function:
       - Set up logging with basic config
       - Call apply_nature_style()
       - Call generate_all_tables(results_dir)
       - For each property in PROPERTIES:
         - Log banner: "=== Generating parity plot: {property} ==="
         - Call plot_parity with placeholder predictions (will need prediction data -- for now, skip if no prediction data available)
       - Call plot_bar_comparison
       - Call plot_learning_curves
       - Log "Evaluation complete. Figures saved to {output_dir}"
    4. `if __name__ == "__main__": main()` guard

    Note: Parity plots require y_true/y_pred arrays which are NOT in the JSON results. For now, the __main__.py generates bar charts and learning curves from existing artifacts. Parity plots will be generated when the pipeline runs inference (Plan 03 integrates this). The plot_parity function is ready to use once predictions are available.

    Update `cathode_ml/evaluation/__init__.py` to export plot functions as well.
  </action>
  <verify>
    <automated>cd D:/GitRepo/lithium-cathode-ml && python -c "from cathode_ml.evaluation.__main__ import main; print('CLI entry point imports OK')"</automated>
  </verify>
  <done>
    - `python -m cathode_ml.evaluation` runs without error (generates tables + bar chart + learning curves from existing results)
    - Evaluation __init__.py exports both metrics and plots public API
  </done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_plots.py tests/test_evaluation.py -x -v` -- all tests pass
- `python -c "from cathode_ml.evaluation.plots import plot_parity, plot_bar_comparison, plot_learning_curves"` imports OK
- `python -c "from cathode_ml.evaluation.__main__ import main"` imports OK
</verification>

<success_criteria>
- Parity plot function generates 2x2 subplot PNG with Wong palette and Nature style
- Bar chart shows model comparison across properties
- Learning curves handle both CGCNN and MEGNet CSV formats (including missing lr column)
- CLI entry point orchestrates all evaluation outputs
- All figures are 300 DPI PNG per locked decision
</success_criteria>

<output>
After completion, create `.planning/phases/05-evaluation-and-benchmarking/05-02-SUMMARY.md`
</output>
