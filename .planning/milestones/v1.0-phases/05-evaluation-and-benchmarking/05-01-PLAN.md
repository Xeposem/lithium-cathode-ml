---
phase: 05-evaluation-and-benchmarking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cathode_ml/evaluation/__init__.py
  - cathode_ml/evaluation/metrics.py
  - tests/test_evaluation.py
autonomous: true
requirements:
  - EVAL-01
  - EVAL-02

must_haves:
  truths:
    - "All model results (RF, XGBoost, CGCNN, MEGNet) load into a unified dict keyed by property then model"
    - "Comparison markdown tables show one table per property with bolded best values"
    - "Comparison JSON contains the same data in machine-readable format"
    - "Missing models or properties are handled gracefully with N/A"
  artifacts:
    - path: "cathode_ml/evaluation/__init__.py"
      provides: "Package init with public API exports"
    - path: "cathode_ml/evaluation/metrics.py"
      provides: "Unified result loading, comparison table generation, prediction re-inference"
      exports: ["load_all_results", "generate_comparison_table", "generate_all_tables", "get_predictions_for_property"]
    - path: "tests/test_evaluation.py"
      provides: "Unit tests for metrics loading, table generation, prediction inference"
  key_links:
    - from: "cathode_ml/evaluation/metrics.py"
      to: "data/results/baselines/baseline_results.json"
      via: "json.load in load_all_results"
      pattern: "json\\.load.*baseline_results"
    - from: "cathode_ml/evaluation/metrics.py"
      to: "data/results/cgcnn/cgcnn_results.json"
      via: "json.load in load_all_results"
      pattern: "json\\.load.*cgcnn_results"
    - from: "cathode_ml/evaluation/metrics.py"
      to: "data/results/megnet/megnet_results.json"
      via: "json.load in load_all_results"
      pattern: "json\\.load.*megnet_results"
---

<objective>
Create the evaluation metrics subpackage that loads JSON result artifacts from all four models (RF, XGBoost, CGCNN, MEGNet), normalizes them into a unified comparison structure, and generates publication-quality markdown + JSON comparison tables.

Purpose: Foundation for all evaluation outputs -- plots and pipeline depend on this unified results loader.
Output: cathode_ml/evaluation/ package with metrics.py, tests proving correct loading and table generation.
</objective>

<execution_context>
@C:/Users/regis/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/regis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-evaluation-and-benchmarking/05-CONTEXT.md
@.planning/phases/05-evaluation-and-benchmarking/05-RESEARCH.md

@cathode_ml/models/utils.py
@cathode_ml/models/baselines.py
@cathode_ml/config.py
@configs/features.yaml

<interfaces>
<!-- Existing interfaces this plan consumes -->

From cathode_ml/models/utils.py:
```python
def compute_metrics(y_true: np.ndarray, y_pred: np.ndarray, n_train: int) -> dict:
    # Returns: {"mae": float, "rmse": float, "r2": float, "n_train": int, "n_test": int}

def save_results(results: dict, path: str) -> None:
    # Writes JSON to path, creates parent dirs
```

From cathode_ml/config.py:
```python
def load_config(config_path: str = "configs/data.yaml") -> dict:
def set_seeds(config: dict) -> None:
def get_project_root() -> Path:
```

JSON result format (baselines): {property: {model_type: {mae, rmse, r2, n_train, n_test}}}
JSON result format (cgcnn): {property: {cgcnn: {mae, rmse, r2, n_train, n_test}}}
JSON result format (megnet): {property: {megnet: {mae, rmse, r2, n_train, n_test}}}

Target properties: formation_energy_per_atom, voltage, capacity, energy_above_hull
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create evaluation metrics module with unified result loading and comparison tables</name>
  <files>cathode_ml/evaluation/__init__.py, cathode_ml/evaluation/metrics.py, tests/test_evaluation.py</files>
  <behavior>
    - test_load_all_results_empty: Returns empty dict when no result files exist
    - test_load_all_results_baselines_only: Correctly loads baseline JSON with rf and xgb nested under properties
    - test_load_all_results_all_models: Loads baselines + cgcnn + megnet into unified {property: {model: metrics}} dict
    - test_load_all_results_missing_model: Gracefully handles missing model directories (no crash)
    - test_generate_comparison_table_format: Output is valid markdown table with header, separator, and data rows
    - test_generate_comparison_table_bold_best: Best MAE/RMSE (lowest) and R2 (highest) are wrapped in ** bold markers
    - test_generate_comparison_table_megnet_dagger: MEGNet label includes dagger symbol and footnote
    - test_generate_comparison_table_missing_model: Missing model produces no row (not crash)
    - test_generate_all_tables: Produces one table per property, writes markdown and JSON files
    - test_model_labels: RF="RF", XGB="XGBoost", CGCNN="CGCNN", MEGNet="MEGNet+" with dagger
  </behavior>
  <action>
    Create `cathode_ml/evaluation/__init__.py` with docstring and public API imports.

    Create `cathode_ml/evaluation/metrics.py` with:

    1. Module-level constants:
       - MODEL_COLORS dict: rf="#0072B2", xgb="#D55E00", cgcnn="#009E73", megnet="#CC79A7" (Wong palette, locked decision)
       - MODEL_LABELS dict: rf="RF", xgb="XGBoost", cgcnn="CGCNN", megnet="MEGNet\u2020" (dagger for pretrained, locked decision)
       - MODELS_ORDER list: ["rf", "xgb", "cgcnn", "megnet"]
       - PROPERTIES list: ["formation_energy_per_atom", "voltage", "capacity", "energy_above_hull"]

    2. `load_all_results(results_base: str = "data/results") -> dict`:
       - Load baselines from {results_base}/baselines/baseline_results.json
       - Load CGCNN from {results_base}/cgcnn/cgcnn_results.json
       - Load MEGNet from {results_base}/megnet/megnet_results.json
       - Normalize into unified dict: {property: {model: {mae, rmse, r2, n_train, n_test}}}
       - Use Path.exists() checks, skip missing files gracefully with logger.warning
       - Return empty dict if nothing found

    3. `generate_comparison_table(all_results: dict, property_name: str) -> str`:
       - Per research CONTEXT.md locked decision: one markdown table per property
       - Models as rows (in MODELS_ORDER), MAE/RMSE/R-squared as columns
       - Bold best value per column: lowest MAE, lowest RMSE, highest R-squared
       - Use `**{value}**` markdown bold syntax
       - Skip models not present for this property
       - Add footnote: "\u2020 Fine-tuned from pretrained MEGNet-MP-2019.4.1"

    4. `generate_all_tables(results_base: str = "data/results") -> None`:
       - Load all results, generate table for each property
       - Write combined markdown to data/results/comparison/comparison.md
       - Write combined JSON to data/results/comparison/comparison.json
       - Create output directories if needed

    Create `tests/test_evaluation.py` with tests per behavior spec above. Use tmp_path fixture to create mock JSON result files for testing. Do NOT import matgl/torch/heavy deps in tests.
  </action>
  <verify>
    <automated>cd D:/GitRepo/lithium-cathode-ml && python -m pytest tests/test_evaluation.py -x -v</automated>
  </verify>
  <done>
    - load_all_results correctly normalizes all three JSON formats into unified dict
    - generate_comparison_table produces valid markdown with bolded best values and MEGNet dagger
    - generate_all_tables writes comparison.md and comparison.json to data/results/comparison/
    - All tests pass
  </done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_evaluation.py -x -v` passes all tests
- `python -c "from cathode_ml.evaluation.metrics import load_all_results, generate_comparison_table"` imports without error
</verification>

<success_criteria>
- Unified result loading handles all three JSON formats (baselines, cgcnn, megnet)
- Markdown tables follow locked decision format: one per property, bolded best, MEGNet dagger
- JSON comparison artifact is machine-readable
- Missing models/properties do not crash the system
</success_criteria>

<output>
After completion, create `.planning/phases/05-evaluation-and-benchmarking/05-01-SUMMARY.md`
</output>
