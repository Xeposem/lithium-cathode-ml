---
phase: 06-dashboard-and-documentation
plan: 04
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - README.md
autonomous: false
requirements: [DOCS-01, DOCS-02, DOCS-03, DOCS-04]

must_haves:
  truths:
    - "README contains project introduction explaining lithium cathode prediction motivation"
    - "README describes methodology covering all four model architectures and evaluation approach"
    - "README explains how to run the full pipeline with CLI commands"
    - "README includes results summary table showing best model per property with MAE and R-squared"
  artifacts:
    - path: "README.md"
      provides: "Complete project documentation"
      min_lines: 150
      contains: "cathode"
  key_links:
    - from: "README.md"
      to: "cathode_ml/pipeline.py"
      via: "Documents CLI usage from pipeline module"
      pattern: "python -m cathode_ml"
---

<objective>
Write a comprehensive README.md with academic portfolio tone covering project introduction, data sources, methodology, results, dashboard usage, and reproduction instructions.

Purpose: Makes the project presentable for ML researchers and hiring managers (DOCS-01 through DOCS-04). The README is the primary entry point for anyone discovering this repository.

Output: Complete README.md at project root.
</objective>

<execution_context>
@C:/Users/regis/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/regis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-dashboard-and-documentation/06-CONTEXT.md
@.planning/phases/06-dashboard-and-documentation/06-01-SUMMARY.md

<interfaces>
<!-- Key info for README content -->

CLI pipeline usage (from cathode_ml/__main__.py -> pipeline.py):
```bash
python -m cathode_ml                    # Full pipeline
python -m cathode_ml --skip-fetch       # Skip data fetching
python -m cathode_ml --skip-train       # Skip training
python -m cathode_ml --models rf xgb    # Specific models only
```

Dashboard launch:
```bash
streamlit run dashboard/app.py
```

Config files: configs/data.yaml, configs/features.yaml, configs/baselines.yaml, configs/cgcnn.yaml, configs/megnet.yaml

Data sources: Materials Project (mp-api), OQMD (REST), Battery Data Genome (CSV)
Models: Random Forest, XGBoost, CGCNN (PyG CGConv), MEGNet (matgl fine-tuned)
Properties: formation_energy_per_atom, voltage, capacity, energy_above_hull
Evaluation: MAE, RMSE, R-squared on compositional group splits
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write comprehensive README.md</name>
  <files>README.md</files>
  <action>
  Write README.md with the following sections. Academic portfolio tone -- written for ML researchers and hiring managers. Clear, concise, professional.

  **## Lithium-Ion Battery Cathode Performance Prediction** (title, with a one-sentence tagline)

  **### Introduction** (DOCS-01):
  - Motivation: predicting cathode material properties (voltage, capacity, formation energy, stability) from crystal structure to accelerate battery materials discovery
  - Approach: compare traditional ML (RF, XGBoost on Magpie descriptors) vs graph neural networks (CGCNN, MEGNet on crystal graphs) on a curated multi-source dataset
  - 2-3 paragraphs max

  **### Data Sources**:
  - Materials Project (mp-api): primary source, lithium cathode electrodes with crystal structures
  - OQMD (REST API): supplementary computed properties
  - Battery Data Genome (CSV): experimental battery data
  - Mention deduplication via source priority (MP > OQMD > BDG) and compositional group splitting to prevent data leakage

  **### Methodology** (DOCS-02):
  - Architecture summaries for each model (2-3 sentences each):
    - Random Forest: Magpie 132-dim composition descriptors, ensemble of decision trees
    - XGBoost: same features, gradient-boosted trees with regularization
    - CGCNN: Crystal Graph Convolutional Neural Network (Xie & Grossman 2018), PyG CGConv layers on atom-bond graphs with Gaussian distance expansion
    - MEGNet: Materials Graph Network fine-tuned from pretrained MEGNet-MP model (matgl), state features for global properties
  - Key design choices: separate models per property, compositional group splitting (no polymorph leakage), config-driven hyperparameters
  - Evaluation: MAE, RMSE, R-squared on held-out test sets with consistent compositional folds

  **### Results** (DOCS-04):
  - Results summary table: one row per property, columns: Property, Best Model, MAE, R-squared. Use placeholder values with a note to update after training (since actual results depend on data availability).
  - Reference the bar comparison chart: `![Model Comparison](data/results/comparison/bar_comparison.png)` (if file exists after evaluation)
  - Brief interpretation: which model types tend to perform better and why (GNNs leverage structural info, baselines are competitive on composition-dominated properties)

  **### Dashboard**:
  - Screenshot placeholder: `<!-- TODO: Add dashboard screenshot after first run -->`
  - Brief description of 6 dashboard pages and what each shows
  - Launch command: `streamlit run dashboard/app.py`

  **### How to Run** (DOCS-03):
  - Prerequisites: Python 3.9+, conda or pip
  - Installation: `pip install -r requirements.txt`
  - Configuration: describe configs/ directory and YAML files
  - Full pipeline: `python -m cathode_ml` with stage descriptions
  - Selective run: `--skip-fetch`, `--skip-train`, `--models` flags
  - Dashboard: `streamlit run dashboard/app.py`
  - Note about Materials Project API key: set `MP_API_KEY` env var or in config

  **### Project Structure**:
  - Directory tree showing key files (cathode_ml/, dashboard/, configs/, data/, tests/)
  - Brief annotation per directory

  **### License** (if applicable, or note "Academic project")

  Keep total length 200-300 lines. Use markdown headers, code blocks for commands, and a clean table for results. Do NOT use emojis.
  </action>
  <verify>
    <automated>python -c "p=open('README.md').read(); sections=['Introduction','Methodology','Results','How to Run']; missing=[s for s in sections if s not in p]; print(f'Missing sections: {missing}') if missing else print('All sections present'); assert not missing"</automated>
  </verify>
  <done>README.md contains Introduction, Data Sources, Methodology, Results (with table), Dashboard, How to Run, and Project Structure sections. Academic tone, 200-300 lines, no emojis.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Review README and dashboard</name>
  <files>README.md</files>
  <action>
  Human reviews the complete README.md for quality, tone, and accuracy, and verifies the full dashboard renders correctly.
  </action>
  <what-built>Complete README.md with academic portfolio tone, methodology section, results table, and reproduction instructions. Also the full 6-page Streamlit dashboard (built across Plans 01-03).</what-built>
  <how-to-verify>
    1. Open README.md and review:
       - Introduction covers motivation clearly
       - Methodology describes all 4 model architectures
       - Results table is present (placeholder values OK if pipeline hasn't been run)
       - How to Run section has clear CLI commands
       - Overall tone is professional/academic
    2. Launch dashboard: `streamlit run dashboard/app.py`
       - Verify sidebar navigation shows 6 pages
       - Check Overview page loads (may show warnings if no results data yet)
       - Check each page renders without errors
  </how-to-verify>
  <verify>Human approval</verify>
  <done>Human confirms README quality and dashboard functionality</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. README.md exists with all required sections
2. `python -c "..."` verification confirms all sections present
3. Human review confirms quality and tone
</verification>

<success_criteria>
- README.md contains all 6 major sections (Introduction, Data Sources, Methodology, Results, Dashboard, How to Run)
- Results summary table present with property/model/metric columns
- Pipeline CLI commands documented correctly
- Dashboard description and launch command included
- Human approves quality and tone
</success_criteria>

<output>
After completion, create `.planning/phases/06-dashboard-and-documentation/06-04-SUMMARY.md`
</output>
