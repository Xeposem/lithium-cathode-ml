---
phase: 06-dashboard-and-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - cathode_ml/models/baselines.py
  - dashboard/utils/model_loader.py
  - dashboard/pages/predict.py
  - dashboard/pages/crystal_viewer.py
  - tests/test_dashboard_predict.py
autonomous: true
requirements: [DASH-02, DASH-07]

must_haves:
  truths:
    - "User can input a composition string and get predicted properties from RF and XGBoost"
    - "User can upload a CIF file and get predictions from all four models"
    - "CIF upload renders an inline 3D crystal structure preview before predictions"
    - "Crystal Viewer page renders any uploaded CIF as interactive 3D structure"
    - "Baseline models are persisted as joblib files after training"
  artifacts:
    - path: "cathode_ml/models/baselines.py"
      provides: "Updated run_baselines with joblib model persistence"
      contains: "joblib.dump"
    - path: "dashboard/utils/model_loader.py"
      provides: "Model loading functions for baselines (joblib) and GNNs (torch)"
      contains: "st.cache_resource"
    - path: "dashboard/pages/predict.py"
      provides: "Dual-mode prediction page (composition + CIF)"
      contains: "file_uploader"
    - path: "dashboard/pages/crystal_viewer.py"
      provides: "3D crystal structure viewer page"
      contains: "showmol"
    - path: "tests/test_dashboard_predict.py"
      provides: "Unit tests for model loading and prediction logic"
  key_links:
    - from: "dashboard/utils/model_loader.py"
      to: "cathode_ml/models/baselines.py"
      via: "joblib.load for persisted baseline models"
      pattern: "joblib\\.load"
    - from: "dashboard/pages/predict.py"
      to: "dashboard/utils/model_loader.py"
      via: "load_baseline_model, load_gnn_model calls"
      pattern: "load_baseline_model|load_gnn_model"
    - from: "dashboard/pages/crystal_viewer.py"
      to: "stmol"
      via: "showmol() for py3Dmol rendering"
      pattern: "showmol"
---

<objective>
Add baseline model persistence (joblib), create the Predict page with dual-mode input (composition string + CIF upload), and create the Crystal Viewer page with py3Dmol 3D rendering.

Purpose: Enables interactive prediction (DASH-02) and crystal visualization (DASH-07). Requires retrofitting baseline model saving since only metric JSONs were previously saved.

Output: Predict page serving predictions from all four model types, Crystal Viewer with 3D structure rendering, and updated baselines.py with joblib persistence.
</objective>

<execution_context>
@C:/Users/regis/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/regis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-dashboard-and-documentation/06-CONTEXT.md
@.planning/phases/06-dashboard-and-documentation/06-RESEARCH.md

<interfaces>
<!-- Key types and contracts from existing codebase -->

From cathode_ml/models/baselines.py:
```python
def train_baseline(X_train, y_train, model_type: str, config: dict, seed: int = 42) -> Any:
    """Returns fitted sklearn/xgboost model instance."""

def run_baselines(records, features_config, baselines_config, seed=42) -> dict:
    """Orchestrates RF + XGBoost training per property. Saves JSON results."""
    # Currently does NOT save model objects -- only metrics JSON
    # Models: rf_model, xgb_model are created but discarded after evaluate_model()
```

From cathode_ml/features/composition.py:
```python
def featurize_compositions(formulas: list[str]) -> tuple[np.ndarray, list[str]]:
    """Returns (X, labels) -- 132-dim Magpie descriptors per formula."""
```

From cathode_ml/models/cgcnn.py:
```python
class CGCNNModel(nn.Module):
    """CGConv layers + FC head. Args: node_feature_dim, edge_feature_dim, hidden_dim, n_conv, n_fc"""
```

From cathode_ml/data/schemas.py:
```python
@dataclass
class MaterialRecord:
    material_id: str; formula: str; structure_dict: dict; source: str
    formation_energy_per_atom: Optional[float]; energy_above_hull: Optional[float]
    voltage: Optional[float]; capacity: Optional[float]
    is_stable: Optional[bool]; space_group: Optional[int]
```

GNN checkpoints: data/results/{cgcnn,megnet}/{model}_{property}_best.pt
  Contains: model_state_dict, optimizer_state_dict, epoch, best_val_loss

Config files: configs/cgcnn.yaml, configs/megnet.yaml, configs/features.yaml
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Baseline model persistence and model loader utilities</name>
  <files>cathode_ml/models/baselines.py, dashboard/utils/model_loader.py, tests/test_dashboard_predict.py</files>
  <behavior>
    - test_baselines_save_joblib: after run_baselines, joblib files exist at data/results/baselines/{model_type}_{property}.joblib
    - test_load_baseline_model: load_baseline_model returns object with .predict() method
    - test_load_baseline_model_missing: returns None and logs warning when file missing
    - test_predict_from_composition: predict_from_composition("LiFePO4") returns dict with property keys and float values
    - test_predict_from_composition_no_models: returns empty dict when no models persisted
  </behavior>
  <action>
  1. Modify `cathode_ml/models/baselines.py` `run_baselines()`:
     - After `rf_model = train_baseline(...)` and `evaluate_model(rf_model, ...)`, add `joblib.dump(rf_model, Path(results_dir) / "baselines" / f"rf_{prop}.joblib")`.
     - Same for xgb_model: `joblib.dump(xgb_model, Path(results_dir) / "baselines" / f"xgb_{prop}.joblib")`.
     - Add `import joblib` (lazy import inside run_baselines to match project pattern -- though joblib is a transitive dep of sklearn so always available).
     - Ensure the baselines directory exists: `(Path(results_dir) / "baselines").mkdir(parents=True, exist_ok=True)` before the property loop.
     - This is backward-compatible: existing JSON saving still works, joblib files are additive.

  2. Create `dashboard/utils/model_loader.py`:
     - `load_baseline_model(model_type: str, property_name: str, results_base="data/results")`: loads joblib file, returns model or None if missing. Use `@st.cache_resource`.
     - `load_gnn_model(model_name: str, property_name: str, results_base="data/results", configs_dir="configs")`: loads .pt checkpoint, reconstructs model from config YAML, loads state_dict, sets eval mode. Use `@st.cache_resource`. Lazy import torch only inside this function. For CGCNN: read configs/cgcnn.yaml + configs/features.yaml, call CGCNNModel(...) with config params, load state_dict. For MEGNet: use lazy matgl import pattern, show clear error if matgl unavailable.
     - `predict_from_composition(formula: str, results_base="data/results") -> dict`: featurize single composition via featurize_compositions([formula]), load all available baseline models, run predict, return {property: {model: value}}.
     - `predict_from_structure(structure, results_base="data/results", configs_dir="configs") -> dict`: convert pymatgen Structure to PyG graph, run through loaded GNN models, return {property: {model: value}}. Lazy imports for torch, torch_geometric.

  3. Create `tests/test_dashboard_predict.py` with behavior tests. Mock joblib.load, torch.load, st.cache_resource (as identity decorator). Use tmp_path for file fixtures.
  </action>
  <verify>
    <automated>python -m pytest tests/test_dashboard_predict.py -x -v</automated>
  </verify>
  <done>run_baselines saves joblib files alongside JSON. Model loader can load baselines and make predictions from composition strings. All 5+ tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Predict page and Crystal Viewer page</name>
  <files>dashboard/pages/predict.py, dashboard/pages/crystal_viewer.py</files>
  <action>
  1. Create `dashboard/pages/predict.py` (DASH-02):
     - Title: "Predict Cathode Properties"
     - Two-column layout: left column for input, right column for results.
     - Input mode 1: `st.text_input("Composition formula", placeholder="e.g. LiFePO4")` with a "Predict" button. On click: call `predict_from_composition(formula)`, display results.
     - Input mode 2: `st.file_uploader("Upload CIF file for GNN predictions", type=["cif"])`. On upload: parse CIF with `Structure.from_str(content, fmt="cif")`, render inline 3D preview using py3Dmol/stmol (confirming correct structure), then call `predict_from_structure(structure)` for GNN predictions. Also run composition-based baselines from the structure's formula.
     - Results display: For each property with predictions, show a "card" using `st.container()` with border. Card shows property name (human-readable title), predicted value with units (V for voltage, mAh/g for capacity, eV/atom for formation energy, eV/atom for energy above hull), and which model(s) produced the prediction.
     - Error handling: wrap CIF parsing in try/except, show `st.error("Invalid CIF file")` on failure. Wrap prediction in try/except, show `st.error()` with traceback.
     - Use lazy imports for torch/pymatgen inside prediction code path only.
     - Property units mapping: `{"voltage": "V", "capacity": "mAh/g", "formation_energy_per_atom": "eV/atom", "energy_above_hull": "eV/atom"}`

  2. Create `dashboard/pages/crystal_viewer.py` (DASH-07):
     - Title: "Crystal Structure Viewer"
     - `st.file_uploader("Upload CIF file", type=["cif"])` for CIF upload.
     - On upload: decode bytes to UTF-8, parse with `Structure.from_str()`, display structure info (formula, space group, lattice params) in sidebar or expander.
     - Render 3D viewer using `py3Dmol.view()` + `stmol.showmol()` with explicit width=700, height=500.
     - Style: sphere+stick (`{"sphere": {"radius": 0.4}, "stick": {"radius": 0.15}}`), color by element (py3Dmol default).
     - Add `st.download_button` to download the parsed CIF back.
     - Handle decode errors and invalid CIF with `st.error()`.
     - If no file uploaded, show example info: "Upload a CIF file to visualize its crystal structure in 3D."
  </action>
  <verify>
    <automated>python -c "import dashboard.pages.predict; import dashboard.pages.crystal_viewer; print('Predict and Crystal Viewer pages importable')"</automated>
  </verify>
  <done>Predict page accepts composition strings (baseline predictions) and CIF uploads (all model predictions with 3D preview). Crystal Viewer page renders uploaded CIF files as interactive 3D structures. Property cards show values with units.</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_dashboard_predict.py -x -v` -- all prediction tests pass
2. `python -m pytest tests/test_dashboard.py tests/test_dashboard_predict.py -x` -- all dashboard tests pass
3. Dashboard pages importable without errors
</verification>

<success_criteria>
- `run_baselines()` saves .joblib model files alongside existing JSON metrics
- Predict page renders with composition input and CIF upload modes
- Crystal Viewer page renders 3D structure from uploaded CIF
- Property predictions displayed as cards with correct units
- CIF upload shows inline 3D preview on Predict page before predictions
</success_criteria>

<output>
After completion, create `.planning/phases/06-dashboard-and-documentation/06-02-SUMMARY.md`
</output>
