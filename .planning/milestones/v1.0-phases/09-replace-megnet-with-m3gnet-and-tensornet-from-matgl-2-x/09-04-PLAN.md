---
phase: 09-replace-megnet-with-m3gnet-and-tensornet-from-matgl-2-x
plan: 04
type: execute
wave: 3
depends_on: [09-01, 09-02]
files_modified:
  - tests/test_m3gnet.py
  - tests/test_tensornet.py
  - tests/test_pipeline.py
  - tests/test_evaluation.py
  - tests/test_plots.py
  - tests/test_dashboard_predict.py
  - cathode_ml/models/megnet.py
  - cathode_ml/models/train_megnet.py
  - configs/megnet.yaml
  - tests/test_megnet.py
autonomous: true
requirements: [MODL-02]

must_haves:
  truths:
    - "test_m3gnet.py tests: lazy imports, pretrained loading (mocked), state dict extraction, Lightning log conversion, artifact format, split consistency, checkpoint naming, per-property training loop"
    - "test_tensornet.py tests: lazy imports, config-driven construction (mocked), state dict extraction, Lightning log conversion, artifact format, from-scratch training loop, element_types handling"
    - "test_pipeline.py default models include m3gnet and tensornet (not megnet)"
    - "test_pipeline.py integration tests mock train_m3gnet and train_tensornet, assert m3gnet.yaml and tensornet.yaml loaded"
    - "test_evaluation.py uses SAMPLE_M3GNET_RESULTS and SAMPLE_TENSORNET_RESULTS (not megnet)"
    - "test_evaluation.py tests dagger label for m3gnet and _M3GNET_FOOTNOTE"
    - "test_plots.py learning curves grid includes m3gnet and tensornet columns (not megnet)"
    - "test_dashboard_predict.py tests m3gnet and tensornet state_dict loading (not megnet)"
    - "Old MEGNet files deleted: megnet.py, train_megnet.py, megnet.yaml, test_megnet.py"
  artifacts:
    - path: "tests/test_m3gnet.py"
      provides: "M3GNet unit tests covering wrapper and training"
      contains: "test_.*m3gnet"
    - path: "tests/test_tensornet.py"
      provides: "TensorNet unit tests covering wrapper and training"
      contains: "test_.*tensornet"
    - path: "tests/test_pipeline.py"
      provides: "Updated pipeline tests with m3gnet and tensornet"
      contains: "m3gnet.*tensornet"
  key_links:
    - from: "tests/test_m3gnet.py"
      to: "cathode_ml/models/m3gnet.py"
      via: "import under test"
      pattern: "from cathode_ml\\.models\\.m3gnet import"
    - from: "tests/test_m3gnet.py"
      to: "cathode_ml/models/train_m3gnet.py"
      via: "import under test"
      pattern: "from cathode_ml\\.models\\.train_m3gnet import"
    - from: "tests/test_tensornet.py"
      to: "cathode_ml/models/tensornet.py"
      via: "import under test"
      pattern: "from cathode_ml\\.models\\.tensornet import"
    - from: "tests/test_tensornet.py"
      to: "cathode_ml/models/train_tensornet.py"
      via: "import under test"
      pattern: "from cathode_ml\\.models\\.train_tensornet import"
---

<objective>
Create M3GNet and TensorNet test suites, update existing tests, and delete old MEGNet files.

Purpose: Complete the migration by replacing all test coverage for MEGNet with equivalent coverage for M3GNet and TensorNet. Delete the old MEGNet source files, config, and tests. After this plan, the full test suite passes with zero MEGNet references.

Output: New test files for M3GNet and TensorNet, updated integration tests, and deletion of 4 MEGNet files.
</objective>

<execution_context>
@C:/Users/regis/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/regis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-replace-megnet-with-m3gnet-and-tensornet-from-matgl-2-x/09-RESEARCH.md
@.planning/phases/09-replace-megnet-with-m3gnet-and-tensornet-from-matgl-2-x/09-01-SUMMARY.md
@.planning/phases/09-replace-megnet-with-m3gnet-and-tensornet-from-matgl-2-x/09-02-SUMMARY.md
@.planning/phases/09-replace-megnet-with-m3gnet-and-tensornet-from-matgl-2-x/09-03-SUMMARY.md

<interfaces>
<!-- Existing test_megnet.py structure to replicate -->

tests/test_megnet.py has 9 test classes:
- TestMEGNetLazyImports: tests _import_matgl, _INSTALL_MSG
- TestLoadMEGNetModel: tests load_megnet_model with mocked matgl
- TestGetAvailableMEGNetModels: tests model listing with mocked matgl
- TestGetMEGNetStateDict: tests state_dict extraction
- TestPredictWithMEGNet: tests predict_with_megnet with mocked model
- TestConvertLightningLogs: tests CSV conversion
- TestTrainMEGNetForProperty: tests single-property training
- TestTrainMEGNet: tests full orchestrator loop
- TestMEGNetArtifactFormat: tests checkpoint naming, results JSON format

<!-- Existing test_pipeline.py megnet references -->
- Default models list includes "megnet"
- Integration tests mock `cathode_ml.models.train_megnet.train_megnet`
- Assertions check `megnet.yaml` is loaded

<!-- Existing test_evaluation.py megnet references -->
- SAMPLE_MEGNET_RESULTS dict
- Tests for megnet results loading in load_all_results
- Tests for dagger label on megnet
- Tests for _MEGNET_FOOTNOTE in comparison tables

<!-- Existing test_plots.py megnet references -->
- gnn_models list includes "megnet"
- Test for MEGNet CSV without lr column

<!-- Existing test_dashboard_predict.py megnet references -->
- test_megnet_raw_state_dict_loading tests MEGNet checkpoint handling
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create M3GNet test suite</name>
  <files>tests/test_m3gnet.py</files>
  <action>
Create `tests/test_m3gnet.py` following the structure of `tests/test_megnet.py` but for M3GNet:

Read `tests/test_megnet.py` first to understand the exact test patterns, mocking strategies, and assertions used. Then create the M3GNet equivalent:

**TestM3GNetLazyImports:**
- Test that `_import_matgl()` calls `import matgl` and returns the module
- Test that `_INSTALL_MSG` mentions matgl>=2.0.0

**TestLoadM3GNetModel:**
- Mock `matgl.load_model` to return a mock with `.cutoff` attribute
- Call `load_m3gnet_model("M3GNet-MP-2018.6.1-Eform")` and verify matgl.load_model was called

**TestGetAvailableM3GNetModels:**
- Mock `matgl.get_available_pretrained_models` returning a mix of model names
- Verify only "M3GNet" models are returned

**TestGetM3GNetStateDict:**
- Create mock model with `model.model.state_dict()` returning a dict
- Verify `get_m3gnet_state_dict` returns it

**TestPredictWithM3GNet:**
- Mock `model.predict_structure` to return fixed values
- Verify `predict_with_m3gnet` returns list of floats

**TestConvertLightningLogs:**
- Test `convert_lightning_logs` function (imported from train_m3gnet)
- Create a temp CSV with Lightning-style columns, verify conversion to standard format

**TestTrainM3GNetForProperty:**
- Mock matgl imports (load_model, Structure2Graph, get_element_list, MGLDataset, ModelLightningModule, Lightning Trainer)
- Verify function calls load_m3gnet_model, runs training, returns metrics dict

**TestTrainM3GNet:**
- Mock the per-property training function
- Verify orchestrator loops over properties, applies compositional splitting, saves results as m3gnet_results.json

**TestM3GNetArtifactFormat:**
- Verify checkpoint naming: `m3gnet_{property}_best.pt`
- Verify results JSON key is `"m3gnet"` not `"megnet"`
- Verify include_line_graph=True is passed to MGLDataset

All tests must mock matgl/DGL (not require actual installation). Use `unittest.mock.patch` at the correct import locations matching lazy import patterns.
  </action>
  <verify>
    <automated>python -m pytest tests/test_m3gnet.py -x -q 2>&1 | tail -10</automated>
  </verify>
  <done>test_m3gnet.py created with comprehensive tests mirroring test_megnet.py structure. All tests mock matgl and pass without matgl installed.</done>
</task>

<task type="auto">
  <name>Task 2: Create TensorNet test suite</name>
  <files>tests/test_tensornet.py</files>
  <action>
Create `tests/test_tensornet.py` following similar structure but with TensorNet-specific differences:

**TestTensorNetLazyImports:**
- Test `_import_matgl()` and `_INSTALL_MSG`

**TestBuildTensorNetFromConfig:**
- Mock `from matgl.models import TensorNet` (the class)
- Call `build_tensornet_from_config(model_config, element_types)` with a config dict
- Verify TensorNet constructor was called with correct params (units, nblocks, cutoff, etc.)
- Verify element_types was passed through

**TestGetTensorNetStateDict:**
- Same pattern as M3GNet

**TestPredictWithTensorNet:**
- Same pattern as M3GNet

**TestConvertLightningLogs:**
- Same as M3GNet (shared function)

**TestTrainTensorNetForProperty:**
- Key difference: verify model is built via build_tensornet_from_config (not loaded from pretrained)
- Mock get_element_list and build_tensornet_from_config
- Verify include_line_graph=False is used

**TestTrainTensorNet:**
- Same orchestrator pattern but results key is "tensornet"
- Results file is tensornet_results.json

**TestTensorNetArtifactFormat:**
- Checkpoint naming: `tensornet_{property}_best.pt`
- Results JSON key is `"tensornet"`
- No pretrained model loading (unlike M3GNet)
  </action>
  <verify>
    <automated>python -m pytest tests/test_tensornet.py -x -q 2>&1 | tail -10</automated>
  </verify>
  <done>test_tensornet.py created with TensorNet-specific tests: config-driven construction, no pretrained loading, include_line_graph=False. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Update existing integration tests</name>
  <files>tests/test_pipeline.py, tests/test_evaluation.py, tests/test_plots.py, tests/test_dashboard_predict.py</files>
  <action>
Read each file fully before editing.

**test_pipeline.py:**
1. Replace `"megnet"` in default models list with `"m3gnet", "tensornet"`
2. Replace mock for `cathode_ml.models.train_megnet.train_megnet` with mocks for:
   - `cathode_ml.models.train_m3gnet.train_m3gnet`
   - `cathode_ml.models.train_tensornet.train_tensornet`
3. Replace assertions that check `megnet.yaml` loaded with assertions for `m3gnet.yaml` and `tensornet.yaml`
4. Update any test names from megnet to m3gnet/tensornet

**test_evaluation.py:**
1. Replace `SAMPLE_MEGNET_RESULTS` with `SAMPLE_M3GNET_RESULTS` and add `SAMPLE_TENSORNET_RESULTS`
2. Update `_create_results_tree` to create `m3gnet/m3gnet_results.json` and `tensornet/tensornet_results.json` instead of `megnet/megnet_results.json`
3. Replace all assertions checking for "megnet" key with "m3gnet" and/or "tensornet"
4. Update dagger label test: check for "M3GNet†" instead of "MEGNet†"
5. Update footnote test: check for `_M3GNET_FOOTNOTE` instead of `_MEGNET_FOOTNOTE`
6. Update import: `from cathode_ml.evaluation.metrics import _M3GNET_FOOTNOTE`

**test_plots.py:**
1. Replace `"megnet"` in gnn_models with `"m3gnet", "tensornet"`
2. Update test for MEGNet CSV without lr column to test M3GNet CSV without lr column
3. Update any assertions referencing megnet columns to m3gnet/tensornet

**test_dashboard_predict.py:**
1. Replace `test_megnet_raw_state_dict_loading` with `test_m3gnet_raw_state_dict_loading`
2. Add `test_tensornet_state_dict_loading` if the existing file tests checkpoint loading
3. Update mock imports from `cathode_ml.models.megnet` to `cathode_ml.models.m3gnet`
  </action>
  <verify>
    <automated>python -m pytest tests/test_pipeline.py tests/test_evaluation.py tests/test_plots.py tests/test_dashboard_predict.py -x -q 2>&1 | tail -10</automated>
  </verify>
  <done>All integration tests updated: pipeline tests mock m3gnet and tensornet training. Evaluation tests use m3gnet/tensornet samples. Plot tests have updated GNN model lists. Dashboard tests verify m3gnet/tensornet loading.</done>
</task>

<task type="auto">
  <name>Task 4: Delete old MEGNet files</name>
  <files>cathode_ml/models/megnet.py, cathode_ml/models/train_megnet.py, configs/megnet.yaml, tests/test_megnet.py</files>
  <action>
Delete the following 4 files that are now replaced:

1. `cathode_ml/models/megnet.py` -- replaced by m3gnet.py + tensornet.py
2. `cathode_ml/models/train_megnet.py` -- replaced by train_m3gnet.py + train_tensornet.py
3. `configs/megnet.yaml` -- replaced by m3gnet.yaml + tensornet.yaml
4. `tests/test_megnet.py` -- replaced by test_m3gnet.py + test_tensornet.py

Use `git rm` to remove from version control.

After deletion, run a grep to verify no remaining imports of the deleted modules:
```bash
grep -rn "from cathode_ml.models.megnet" cathode_ml/ dashboard/ tests/
grep -rn "from cathode_ml.models.train_megnet" cathode_ml/ dashboard/ tests/
grep -rn "megnet.yaml" cathode_ml/ dashboard/ tests/ configs/
```

Any hits indicate a missed update in Plans 02 or 03 that must be fixed here.
  </action>
  <verify>
    <automated>python -c "from pathlib import Path; assert not Path('cathode_ml/models/megnet.py').exists(); assert not Path('cathode_ml/models/train_megnet.py').exists(); assert not Path('configs/megnet.yaml').exists(); assert not Path('tests/test_megnet.py').exists(); print('All MEGNet files deleted')"</automated>
  </verify>
  <done>All 4 MEGNet files deleted. No remaining imports of megnet.py, train_megnet.py, or references to megnet.yaml in the codebase.</done>
</task>

<task type="auto">
  <name>Task 5: Full test suite verification</name>
  <files></files>
  <action>
Run the complete test suite and fix any failures:

```bash
python -m pytest tests/ -x -q
```

Common issues to watch for:
- Stale imports of megnet modules in any file
- Missing mocks for m3gnet/tensornet in integration tests
- Incorrect model key names in assertions
- Import errors from deleted megnet.py

Also verify no "megnet" string remains anywhere in the active codebase (excluding .planning/ directory and git history):
```bash
grep -rn "megnet" cathode_ml/ dashboard/ tests/ configs/ --include="*.py" --include="*.yaml"
```
Any hits must be fixed.

Check README.md too:
```bash
grep -in "megnet" README.md
```
Any remaining MEGNet references (other than historical context like "previously used MEGNet") must be updated.
  </action>
  <verify>
    <automated>python -m pytest tests/ -x -q 2>&1 | tail -10</automated>
  </verify>
  <done>Full test suite passes. Zero MEGNet references remain in active codebase (cathode_ml/, dashboard/, tests/, configs/). Migration complete.</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_m3gnet.py -x -q` -- M3GNet tests pass
2. `python -m pytest tests/test_tensornet.py -x -q` -- TensorNet tests pass
3. `python -m pytest tests/test_pipeline.py -x -q` -- pipeline tests pass
4. `python -m pytest tests/test_evaluation.py -x -q` -- evaluation tests pass
5. `python -m pytest tests/ -x -q` -- full suite green
6. `grep -rn "megnet" cathode_ml/ dashboard/ tests/ configs/ --include="*.py" --include="*.yaml" | wc -l` -- returns 0
7. `test ! -f cathode_ml/models/megnet.py` -- megnet.py deleted
8. `test ! -f cathode_ml/models/train_megnet.py` -- train_megnet.py deleted
9. `test ! -f configs/megnet.yaml` -- megnet.yaml deleted
10. `test ! -f tests/test_megnet.py` -- test_megnet.py deleted
</verification>

<success_criteria>
- test_m3gnet.py has comprehensive tests covering wrapper + training orchestrator
- test_tensornet.py has comprehensive tests covering wrapper + training orchestrator
- All integration tests updated from megnet to m3gnet/tensornet
- All 4 old MEGNet files deleted
- Full test suite passes with zero failures
- Zero "megnet" string occurrences in active codebase
</success_criteria>

<output>
After completion, create `.planning/phases/09-replace-megnet-with-m3gnet-and-tensornet-from-matgl-2-x/09-04-SUMMARY.md`
</output>
