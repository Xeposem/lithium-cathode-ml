---
phase: 09-replace-megnet-with-m3gnet-and-tensornet-from-matgl-2-x
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cathode_ml/models/m3gnet.py
  - cathode_ml/models/tensornet.py
  - configs/m3gnet.yaml
  - configs/tensornet.yaml
  - requirements.txt
autonomous: true
requirements: [MODL-02]

must_haves:
  truths:
    - "m3gnet.py provides load_m3gnet_model, get_available_m3gnet_models, get_m3gnet_state_dict, predict_with_m3gnet with lazy matgl imports"
    - "tensornet.py provides build_tensornet_from_config, get_tensornet_state_dict, predict_with_tensornet with lazy matgl imports"
    - "M3GNet wrapper loads pretrained M3GNet-MP-2018.6.1-Eform via matgl.load_model"
    - "TensorNet wrapper constructs model from config params (no pretrained -- train from scratch)"
    - "Both wrappers use predict_structure() API for inference"
    - "Both wrappers use centralized _import_matgl() lazy import helper"
    - "configs/m3gnet.yaml has pretrained_model, training params (lr=0.001, batch_size=128, n_epochs=1000), results_dir"
    - "configs/tensornet.yaml has architecture params (units, nblocks, cutoff, equivariance group), training params, results_dir"
    - "requirements.txt pins matgl>=2.0.0 and includes lightning>=2.0.0"
  artifacts:
    - path: "cathode_ml/models/m3gnet.py"
      provides: "M3GNet model wrapper with lazy imports"
      contains: "load_m3gnet_model"
    - path: "cathode_ml/models/tensornet.py"
      provides: "TensorNet model wrapper with lazy imports"
      contains: "build_tensornet_from_config"
    - path: "configs/m3gnet.yaml"
      provides: "M3GNet training configuration"
      contains: "M3GNet-MP-2018.6.1-Eform"
    - path: "configs/tensornet.yaml"
      provides: "TensorNet architecture and training configuration"
      contains: "equivariance_invariance_group"
    - path: "requirements.txt"
      provides: "Updated dependency pins for matgl 2.x"
      contains: "matgl>=2\\.0\\.0"
  key_links:
    - from: "cathode_ml/models/m3gnet.py"
      to: "matgl"
      via: "lazy import of matgl.load_model"
      pattern: "matgl\\.load_model"
    - from: "cathode_ml/models/tensornet.py"
      to: "matgl.models"
      via: "lazy import of TensorNet class"
      pattern: "from matgl\\.models import"
---

<objective>
Create M3GNet and TensorNet model wrappers, YAML configs, and update dependencies for matgl 2.x.

Purpose: Establish the model loading/prediction layer for both new architectures before building training orchestrators. M3GNet replaces MEGNet as the pretrained fine-tunable model; TensorNet adds an O(3)-equivariant model trained from scratch.

Output: Two model wrapper modules, two YAML config files, and updated requirements.txt. No training yet -- just model construction, loading, and prediction APIs.
</objective>

<execution_context>
@C:/Users/regis/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/regis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-replace-megnet-with-m3gnet-and-tensornet-from-matgl-2-x/09-RESEARCH.md

<interfaces>
<!-- Existing MEGNet wrapper pattern to follow -->

From cathode_ml/models/megnet.py (pattern to replicate):
```python
def _import_matgl():
    """Lazy import of matgl with helpful error message."""
    try:
        import matgl
    except ImportError:
        raise ImportError(_INSTALL_MSG)
    return matgl

def load_megnet_model(model_name: str):
    matgl = _import_matgl()
    model = matgl.load_model(model_name)
    return model

def predict_with_megnet(model, structures: list) -> List[float]:
    _import_matgl()
    predictions = []
    for struct in structures:
        pred = model.predict_structure(struct)
        predictions.append(float(pred))
    return predictions
```

From matgl 2.x API (from research):
```python
# M3GNet loading (auto-switches to DGL backend)
model = matgl.load_model("M3GNet-MP-2018.6.1-Eform")
prediction = model.predict_structure(structure)

# TensorNet construction (PyG or DGL)
from matgl.models import TensorNet
from matgl.ext.pymatgen import get_element_list
model = TensorNet(
    element_types=elem_list,
    is_intensive=True,
    units=64, nblocks=2, num_rbf=32,
    cutoff=5.0, readout_type="weighted_atom",
    equivariance_invariance_group="O(3)",
    ntargets=1,
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create M3GNet model wrapper</name>
  <files>cathode_ml/models/m3gnet.py</files>
  <action>
Create `cathode_ml/models/m3gnet.py` following the exact pattern of `megnet.py`:

1. Module docstring: "M3GNet model wrapper with lazy matgl imports." Mention it provides functions for loading pretrained M3GNet models, listing available models, extracting state dicts, and running predictions.

2. `_INSTALL_MSG` constant: Update message to say "matgl>=2.0.0 is required for M3GNet training. Install with: pip install 'matgl>=2.0.0' dgl"

3. `_import_matgl()`: Identical lazy import helper as megnet.py.

4. `load_m3gnet_model(model_name: str)`:
   - Call `matgl.load_model(model_name)`
   - Log model name and cutoff
   - Return model object
   - Docstring: mention default model is "M3GNet-MP-2018.6.1-Eform"

5. `get_available_m3gnet_models() -> List[str]`:
   - Filter `matgl.get_available_pretrained_models()` for names containing "M3GNet"
   - Return list of model name strings

6. `get_m3gnet_state_dict(model) -> dict`:
   - Return `model.model.state_dict()`
   - Same as MEGNet pattern (matgl models wrap inner model)

7. `predict_with_m3gnet(model, structures: list) -> List[float]`:
   - Call `_import_matgl()` to ensure available
   - Loop over structures, call `model.predict_structure(struct)`
   - Return list of float predictions
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('cathode_ml/models/m3gnet.py').read()); print('m3gnet.py syntax OK')"</automated>
  </verify>
  <done>m3gnet.py created with load_m3gnet_model, get_available_m3gnet_models, get_m3gnet_state_dict, predict_with_m3gnet. All matgl imports are lazy.</done>
</task>

<task type="auto">
  <name>Task 2: Create TensorNet model wrapper</name>
  <files>cathode_ml/models/tensornet.py</files>
  <action>
Create `cathode_ml/models/tensornet.py` with a different pattern from M3GNet since TensorNet has no pretrained property models and must be built from config:

1. Module docstring: "TensorNet model wrapper with lazy matgl imports." Mention it provides functions for constructing TensorNet models from config, extracting state dicts, and running predictions. Note TensorNet is O(3)-equivariant and trained from scratch.

2. `_INSTALL_MSG`: "matgl>=2.0.0 is required for TensorNet training. Install with: pip install 'matgl>=2.0.0'"

3. `_import_matgl()`: Same lazy import helper.

4. `build_tensornet_from_config(model_config: dict, element_types: list)`:
   - Lazy import `from matgl.models import TensorNet`
   - Extract params from model_config dict: units (default 64), nblocks (default 2), num_rbf (default 32), cutoff (default 5.0), rbf_type (default "Gaussian"), readout_type (default "weighted_atom"), activation_type (default "swish"), equivariance_invariance_group (default "O(3)"), is_intensive (default True), ntargets (default 1)
   - Construct and return `TensorNet(element_types=element_types, **extracted_params)`
   - Log model construction details

5. `get_tensornet_state_dict(model) -> dict`:
   - Return `model.model.state_dict()` (same matgl wrapper pattern)

6. `predict_with_tensornet(model, structures: list) -> List[float]`:
   - Same pattern as M3GNet: loop, predict_structure, return floats
   - Note: TensorNet also has predict_structure() in matgl
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('cathode_ml/models/tensornet.py').read()); print('tensornet.py syntax OK')"</automated>
  </verify>
  <done>tensornet.py created with build_tensornet_from_config, get_tensornet_state_dict, predict_with_tensornet. All matgl imports are lazy. Config-driven construction pattern matches CGCNN's build_cgcnn_from_config.</done>
</task>

<task type="auto">
  <name>Task 3: Create M3GNet and TensorNet YAML configs</name>
  <files>configs/m3gnet.yaml, configs/tensornet.yaml</files>
  <action>
Create `configs/m3gnet.yaml`:
```yaml
# M3GNet model configuration for cathode property prediction
# Fine-tunes pretrained M3GNet from matgl 2.x
# M3GNet uses 3-body interactions (requires include_line_graph=True in training)

model:
  pretrained_model: "M3GNet-MP-2018.6.1-Eform"
  # Architecture params inherited from pretrained model -- do not override
  # dim_node_embedding: 64
  # dim_edge_embedding: 64
  # nblocks: 3
  # units: 64
  # cutoff: 5.0
  # threebody_cutoff: 4.0
  # readout_type: "set2set"
  # is_intensive: true

training:
  learning_rate: 0.001         # matgl 2.x default (CosineAnnealingLR)
  batch_size: 128
  n_epochs: 1000
  early_stopping_patience: 100
  loss: "mse_loss"
  decay_steps: 1000            # CosineAnnealingLR T_max
  decay_alpha: 0.01            # Minimum LR fraction

results_dir: "data/results/m3gnet"
```

Create `configs/tensornet.yaml`:
```yaml
# TensorNet model configuration for cathode property prediction
# Trained from scratch -- no pretrained property prediction models available
# O(3)-equivariant tensor network architecture

model:
  units: 64
  nblocks: 2
  num_rbf: 32
  cutoff: 5.0
  rbf_type: "Gaussian"
  readout_type: "weighted_atom"
  activation_type: "swish"
  equivariance_invariance_group: "O(3)"
  is_intensive: true
  ntargets: 1

training:
  learning_rate: 0.001
  batch_size: 128
  n_epochs: 1000
  early_stopping_patience: 100
  loss: "mse_loss"
  decay_steps: 1000
  decay_alpha: 0.01

results_dir: "data/results/tensornet"
```
  </action>
  <verify>
    <automated>python -c "import yaml; yaml.safe_load(open('configs/m3gnet.yaml')); yaml.safe_load(open('configs/tensornet.yaml')); print('Both YAML configs valid')"</automated>
  </verify>
  <done>m3gnet.yaml and tensornet.yaml created with appropriate training parameters. M3GNet references pretrained model; TensorNet specifies full architecture params for from-scratch training.</done>
</task>

<task type="auto">
  <name>Task 4: Update requirements.txt for matgl 2.x</name>
  <files>requirements.txt</files>
  <action>
Update `requirements.txt`:

1. Change `matgl>=1.1.0` to `matgl>=2.0.0`
2. Ensure `dgl>=2.0.0` is present (needed for M3GNet DGL backend)
3. Ensure `torch-geometric>=2.6.0` is present (needed for CGCNN and TensorNet PyG)
4. Add `lightning>=2.0.0` if not already present (matgl 2.x uses `import lightning as L`)
5. Keep all other dependencies unchanged
  </action>
  <verify>
    <automated>python -c "lines=open('requirements.txt').readlines(); assert any('matgl>=2.0.0' in l for l in lines), 'matgl pin missing'; assert any('dgl' in l for l in lines), 'dgl missing'; assert any('lightning' in l for l in lines), 'lightning missing'; print('requirements.txt OK')"</automated>
  </verify>
  <done>requirements.txt updated: matgl>=2.0.0, dgl>=2.0.0, torch-geometric>=2.6.0, lightning>=2.0.0 all present.</done>
</task>

</tasks>

<verification>
1. `python -c "import ast; ast.parse(open('cathode_ml/models/m3gnet.py').read()); print('OK')"` -- m3gnet.py is valid Python
2. `python -c "import ast; ast.parse(open('cathode_ml/models/tensornet.py').read()); print('OK')"` -- tensornet.py is valid Python
3. `python -c "import yaml; yaml.safe_load(open('configs/m3gnet.yaml')); print('OK')"` -- m3gnet.yaml is valid YAML
4. `python -c "import yaml; yaml.safe_load(open('configs/tensornet.yaml')); print('OK')"` -- tensornet.yaml is valid YAML
5. `grep -n "matgl>=2.0.0" requirements.txt` -- matgl pinned to v2.x
6. `grep -n "lightning" requirements.txt` -- lightning dependency present
7. `grep -n "load_m3gnet_model" cathode_ml/models/m3gnet.py` -- M3GNet loader exists
8. `grep -n "build_tensornet_from_config" cathode_ml/models/tensornet.py` -- TensorNet builder exists
</verification>

<success_criteria>
- m3gnet.py has load_m3gnet_model, get_available_m3gnet_models, get_m3gnet_state_dict, predict_with_m3gnet
- tensornet.py has build_tensornet_from_config, get_tensornet_state_dict, predict_with_tensornet
- All matgl imports are lazy (inside function bodies, not module-level)
- m3gnet.yaml references M3GNet-MP-2018.6.1-Eform pretrained model
- tensornet.yaml specifies full architecture params for from-scratch construction
- requirements.txt pins matgl>=2.0.0 with lightning>=2.0.0
</success_criteria>

<output>
After completion, create `.planning/phases/09-replace-megnet-with-m3gnet-and-tensornet-from-matgl-2-x/09-01-SUMMARY.md`
</output>
