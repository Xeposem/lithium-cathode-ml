---
phase: 04-megnet-implementation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cathode_ml/models/megnet.py
  - configs/megnet.yaml
  - tests/test_megnet.py
autonomous: true
requirements: [MODL-02]

must_haves:
  truths:
    - "MEGNet pretrained model loads via matgl with lazy imports"
    - "MEGNet config YAML contains MEGNet-specific hyperparameters independent of CGCNN"
    - "Test scaffolding exists with skipif markers for missing matgl dependency"
  artifacts:
    - path: "cathode_ml/models/megnet.py"
      provides: "MEGNet model loading, prediction, state_dict extraction"
      exports: ["load_megnet_model", "predict_with_megnet", "get_megnet_state_dict"]
    - path: "configs/megnet.yaml"
      provides: "MEGNet-specific hyperparameters for fine-tuning"
      contains: "pretrained_model"
    - path: "tests/test_megnet.py"
      provides: "Test scaffolding for MODL-02 with skipif matgl"
  key_links:
    - from: "cathode_ml/models/megnet.py"
      to: "matgl"
      via: "lazy import inside functions"
      pattern: "import matgl"
    - from: "configs/megnet.yaml"
      to: "cathode_ml/models/megnet.py"
      via: "config values consumed by training orchestrator"
---

<objective>
Create the MEGNet model wrapper module, YAML configuration, and test scaffolding.

Purpose: Establish the MEGNet interface layer that wraps matgl's pretrained MEGNet model with lazy imports for environment isolation, plus the configuration and test infrastructure that Plan 02 builds upon.
Output: megnet.py wrapper, megnet.yaml config, test_megnet.py with skipif markers
</objective>

<execution_context>
@C:/Users/regis/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/regis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-megnet-implementation/04-CONTEXT.md
@.planning/phases/04-megnet-implementation/04-RESEARCH.md
@.planning/phases/04-megnet-implementation/04-VALIDATION.md

@cathode_ml/models/utils.py
@cathode_ml/models/train_cgcnn.py
@configs/cgcnn.yaml
@configs/features.yaml

<interfaces>
<!-- Key types and contracts the executor needs -->

From cathode_ml/models/utils.py:
```python
def compute_metrics(y_true: np.ndarray, y_pred: np.ndarray, n_train: int) -> dict:
    """Returns dict with keys: mae, rmse, r2, n_train, n_test."""

def save_results(results: dict, path: str) -> None:
    """Save results dictionary to JSON file."""
```

From cathode_ml/features/split.py:
```python
def compositional_split(n_samples, groups, test_size, val_size, seed) -> Tuple[list, list, list]:
    """Returns train_idx, val_idx, test_idx."""

def get_group_keys(formulas: list) -> list:
    """Returns group keys from reduced formulas."""
```

From cathode_ml/data/schemas.py:
```python
@dataclass
class MaterialRecord:
    material_id: str
    formula: str
    structure_dict: dict
    source: str
    formation_energy_per_atom: Optional[float] = None
    voltage: Optional[float] = None
    capacity: Optional[float] = None
    energy_above_hull: Optional[float] = None
```

From configs/features.yaml:
```yaml
target_properties:
  - formation_energy_per_atom
  - voltage
  - capacity
  - energy_above_hull

splitting:
  train_size: 0.8
  val_size: 0.1
  test_size: 0.1
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MEGNet wrapper module with lazy matgl imports</name>
  <files>cathode_ml/models/megnet.py, tests/test_megnet.py</files>
  <behavior>
    - Test 1 (test_lazy_import_error): Calling load_megnet_model when matgl is not installed raises ImportError with install instructions mentioning "pip install matgl==1.3.0 dgl==2.2.0"
    - Test 2 (test_load_pretrained): When matgl IS available, load_megnet_model("MEGNet-MP-2018.6.1-Eform") returns a matgl model object with a .cutoff attribute (skip if matgl not installed)
    - Test 3 (test_get_available_models): get_available_megnet_models() returns a list of strings filtered to MEGNet models only (skip if matgl not installed)
    - Test 4 (test_get_state_dict): get_megnet_state_dict(model) returns a dict-like object suitable for torch.save (skip if matgl not installed)
  </behavior>
  <action>
Create cathode_ml/models/megnet.py with the following functions, ALL using lazy imports for matgl/DGL inside the function body (never at module top level), following the exact pattern from cathode_ml/models/baselines.py (xgboost lazy import):

1. `load_megnet_model(model_name: str)` - Lazy imports matgl, calls matgl.load_model(model_name), returns the model. On ImportError, raises with message: "matgl is required for MEGNet training. Install with: pip install matgl==1.3.0 dgl==2.2.0"

2. `get_available_megnet_models() -> list` - Lazy imports matgl, calls matgl.get_available_pretrained_models(), filters to only those containing "MEGNet", returns list of strings.

3. `get_megnet_state_dict(model) -> dict` - Extracts and returns model.model.state_dict() (the inner torch Module's state dict) for saving as .pt file (because Lightning .ckpt format differs from project standard .pt).

4. `predict_with_megnet(model, structures: list) -> list` - Lazy imports matgl, calls model.predict_structure(s) for each structure in list, returns list of float predictions. This is for test-set evaluation after training.

Create tests/test_megnet.py with:
- `HAS_MATGL = importlib.util.find_spec("matgl") is not None` at top
- `skip_no_matgl = pytest.mark.skipif(not HAS_MATGL, reason="matgl not installed")`
- Tests for lazy import error behavior (always runs, patches import)
- Tests for model loading, available models, state dict (decorated with @skip_no_matgl)
  </action>
  <verify>
    <automated>pytest tests/test_megnet.py -x -v</automated>
  </verify>
  <done>megnet.py has 4 exported functions with lazy matgl imports; test_megnet.py has 4+ tests, all passing (matgl-dependent tests skip gracefully when matgl absent)</done>
</task>

<task type="auto">
  <name>Task 2: MEGNet YAML configuration</name>
  <files>configs/megnet.yaml</files>
  <action>
Create configs/megnet.yaml with MEGNet-specific hyperparameters matching the template from 04-RESEARCH.md but adjusted per user decisions:

```yaml
# MEGNet model configuration for cathode property prediction
# Fine-tunes pretrained MEGNet from matgl
# NOTE: Architecture params inherited from pretrained model -- do not override

model:
  pretrained_model: "MEGNet-MP-2018.6.1-Eform"  # Verified via get_available_pretrained_models()
  # If this model name is not found at runtime, train_megnet will list available
  # MEGNet models and select the closest match

training:
  optimizer: adam
  learning_rate: 0.0001       # Lower LR for fine-tuning (vs CGCNN's 0.001)
  weight_decay: 0.0
  batch_size: 128             # Larger batches for MEGNet (vs CGCNN's 64)
  n_epochs: 1000              # More epochs with lower LR (vs CGCNN's 400)
  early_stopping_patience: 100  # More patience due to lower LR
  scheduler:
    type: reduce_on_plateau
    factor: 0.5
    patience: 50
    min_lr: 1.0e-7

results_dir: "data/results/megnet"
```

This follows the exact same structure as configs/cgcnn.yaml (model section, training section, results_dir) but with MEGNet-appropriate values per user decision (LR ~1e-4, ~1000 epochs, batch 128).
  </action>
  <verify>
    <automated>python -c "import yaml; d=yaml.safe_load(open('configs/megnet.yaml')); assert d['model']['pretrained_model']; assert d['training']['learning_rate'] == 0.0001; assert d['training']['n_epochs'] == 1000; assert d['results_dir'] == 'data/results/megnet'; print('OK')"</automated>
  </verify>
  <done>megnet.yaml exists with pretrained model name, LR 1e-4, 1000 epochs, batch 128, results_dir data/results/megnet</done>
</task>

</tasks>

<verification>
- `pytest tests/test_megnet.py -x -v` passes (all tests green or skipped appropriately)
- `python -c "from cathode_ml.models.megnet import load_megnet_model, get_available_megnet_models, get_megnet_state_dict, predict_with_megnet"` imports without error (lazy imports mean no matgl needed at import time)
- `python -c "import yaml; yaml.safe_load(open('configs/megnet.yaml'))"` parses without error
- `pytest tests/ -x` full suite still passes (no regressions)
</verification>

<success_criteria>
- megnet.py exports 4 functions with lazy matgl imports
- megnet.yaml has MEGNet-specific hyperparameters (LR 1e-4, 1000 epochs, batch 128)
- test_megnet.py has tests with @skipif for matgl dependency
- Importing megnet module does NOT require matgl to be installed
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-megnet-implementation/04-01-SUMMARY.md`
</output>
