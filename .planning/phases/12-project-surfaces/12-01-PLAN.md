---
phase: 12-project-surfaces
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - SURF-01
  - SURF-02

must_haves:
  truths:
    - "README lists all 4 data sources: Materials Project, OQMD, AFLOW, JARVIS"
    - "README results table shows actual model metrics from the 4-source retraining"
    - "README project structure reflects current files (no bdg_fetcher.py, has aflow_fetcher.py and jarvis_fetcher.py)"
    - "README pipeline description references 4 sources, not 3"
    - "No mention of Battery Data Genome remains in README"
    - "GitHub repo description mentions lithium cathode ML with multiple data sources"
    - "GitHub repo topics include relevant keywords"
  artifacts:
    - path: "README.md"
      provides: "Accurate project documentation"
      contains: "AFLOW"
  key_links:
    - from: "README.md results table"
      to: "data/results/baselines/baseline_results.json"
      via: "manually transcribed metrics"
      pattern: "0\\.9952"
---

<objective>
Update README.md to reflect the current state of the project (4 data sources, corrected model metrics, current file structure) and update the GitHub repo metadata (description + topics) via `gh repo edit`.

Purpose: Ensure anyone visiting the repo sees accurate, current information about the project.
Output: Updated README.md, updated GitHub repo metadata.
</objective>

<execution_context>
@C:/Users/regis/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/regis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@README.md
@data/results/baselines/baseline_results.json
@data/results/comparison/comparison.md
</context>

<interfaces>
<!-- Current metrics from baseline_results.json and comparison data -->

Best models per property (from 4-source retraining, 46,389 records):
- formation_energy_per_atom: CGCNN R2=0.9952, MAE=0.0341
- voltage: XGBoost R2=0.6791, MAE=0.4336
- capacity: CGCNN R2=0.4652, MAE=48.78 (XGBoost close: R2=0.4351, MAE=49.22)
- energy_above_hull: CGCNN R2=0.6903, MAE=0.0211

XGBoost metrics (from baseline_results.json, NOT in current comparison.md):
- formation_energy: MAE=0.0713, R2=0.9853
- voltage: MAE=0.4336, R2=0.6791
- capacity: MAE=49.22, R2=0.4351
- energy_above_hull: MAE=0.0296, R2=0.3858

Current data module files (no bdg_fetcher.py):
- cathode_ml/data/mp_fetcher.py
- cathode_ml/data/oqmd_fetcher.py
- cathode_ml/data/aflow_fetcher.py
- cathode_ml/data/jarvis_fetcher.py
- cathode_ml/data/fetch.py
- cathode_ml/data/clean.py
- cathode_ml/data/cache.py
- cathode_ml/data/schemas.py
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md with corrected content</name>
  <files>README.md</files>
  <action>
Read README.md fully, then rewrite the following sections (preserve all other sections unchanged):

1. **Data Sources section**: Replace the 3-source description with 4 sources. Remove all Battery Data Genome / BDG references. Add:
   - **AFLOW** (REST API): Automatic Flow for Materials Discovery database providing DFT-computed formation energies and structural data for lithium-containing compounds. Accessed via the AFLOW REST API.
   - **JARVIS** (REST API): Joint Automated Repository for Various Integrated Simulations providing DFT properties computed with OptB88vdW functional. Accessed via the JARVIS-Tools API.
   Keep existing MP and OQMD descriptions. Update dedup priority to: Materials Project > OQMD > AFLOW > JARVIS.

2. **Results Summary Table**: Replace placeholder rows with actual best-model metrics:
   - formation_energy_per_atom: CGCNN, MAE 0.0341, R2 0.9952
   - voltage: XGBoost, MAE 0.4336, R2 0.6791
   - capacity: XGBoost, MAE 49.2205, R2 0.4351
   - energy_above_hull: CGCNN, MAE 0.0211, R2 0.6903
   Remove the placeholder note ("Values above are placeholders..."). Keep the interpretation paragraph but update it to mention 46,389 records from 4 data sources.

3. **Project Structure tree**: Remove `bdg_fetcher.py` line. Add:
   - `aflow_fetcher.py` with comment "# AFLOW REST client"
   - `jarvis_fetcher.py` with comment "# JARVIS-DFT client"

4. **Pipeline stages description** (under "Running the Pipeline"): Change step 1 from "Download and cache data from Materials Project, OQMD, and Battery Data Genome" to "Download and cache data from Materials Project, OQMD, AFLOW, and JARVIS".

5. **Introduction paragraph**: Where it says "multi-source dataset" ensure it does not reference a specific count of sources that is wrong. The second paragraph already says "two families" which is about models, not sources -- leave it.

6. Remove the HTML comment "TODO: Add dashboard screenshot after first run" from the Dashboard section.
  </action>
  <verify>
    <automated>python -c "
readme = open('README.md').read()
assert 'Battery Data Genome' not in readme, 'Stale BDG reference found'
assert 'bdg_fetcher' not in readme, 'Stale bdg_fetcher reference found'
assert 'AFLOW' in readme, 'AFLOW not mentioned'
assert 'JARVIS' in readme, 'JARVIS not mentioned'
assert 'aflow_fetcher.py' in readme, 'aflow_fetcher.py not in structure'
assert 'jarvis_fetcher.py' in readme, 'jarvis_fetcher.py not in structure'
assert '0.9952' in readme, 'CGCNN formation energy R2 not in results'
assert '0.6791' in readme, 'XGBoost voltage R2 not in results'
assert 'placeholder' not in readme.lower(), 'Placeholder text remains'
assert 'TODO' not in readme, 'TODO comment remains'
print('All README checks passed')
"
    </automated>
  </verify>
  <done>README.md accurately describes 4 data sources, shows real model metrics, has correct project structure, no stale BDG or placeholder references</done>
</task>

<task type="auto">
  <name>Task 2: Update GitHub repo description and topics</name>
  <files></files>
  <action>
Run the following gh commands to update the repo metadata:

1. Update repo description:
   gh repo edit --description "Predicting lithium cathode properties (voltage, capacity, formation energy, stability) from crystal structure using CGCNN, M3GNet, TensorNet, Random Forest, and XGBoost on data from Materials Project, OQMD, AFLOW, and JARVIS"

2. Update repo topics to include relevant keywords:
   gh repo edit --add-topic machine-learning --add-topic materials-science --add-topic graph-neural-network --add-topic battery --add-topic lithium-ion --add-topic cgcnn --add-topic crystal-structure --add-topic materials-project --add-topic xgboost --add-topic streamlit-dashboard

If `gh` is not authenticated or errors out, create a checkpoint:human-action task noting the exact commands for the user to run. But try the commands first.
  </action>
  <verify>
    <automated>gh repo view --json description,repositoryTopics --jq ".description" 2>/dev/null || echo "gh not available -- verify manually"</automated>
  </verify>
  <done>GitHub repo description mentions all 4 data sources and model types; topics include relevant keywords for discoverability</done>
</task>

</tasks>

<verification>
- README.md contains no references to Battery Data Genome or bdg_fetcher
- README.md mentions AFLOW, JARVIS, Materials Project, OQMD
- README.md results table has real numbers (not placeholders)
- GitHub repo description updated (or commands documented for user)
</verification>

<success_criteria>
- README describes all 4 data sources with accurate model metrics
- No stale v1.0 references remain in README
- GitHub repo metadata reflects current project state
</success_criteria>

<output>
After completion, create `.planning/phases/12-project-surfaces/12-01-SUMMARY.md`
</output>
