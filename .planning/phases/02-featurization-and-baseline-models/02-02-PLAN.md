---
phase: 02-featurization-and-baseline-models
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - cathode_ml/features/composition.py
  - cathode_ml/features/split.py
  - tests/test_composition.py
  - tests/test_split.py
  - requirements.txt
autonomous: true
requirements: [FEAT-03, FEAT-04]

must_haves:
  truths:
    - "Each composition produces a Magpie descriptor vector via matminer"
    - "NaN values in Magpie output are handled (median imputation)"
    - "Train/val/test splits group all entries sharing a reduced composition into the same fold"
    - "No reduced formula appears in both train and test sets"
  artifacts:
    - path: "cathode_ml/features/composition.py"
      provides: "Magpie composition featurizer"
      exports: ["featurize_compositions"]
    - path: "cathode_ml/features/split.py"
      provides: "Compositional group splitter"
      exports: ["get_group_keys", "compositional_split"]
    - path: "tests/test_composition.py"
      provides: "Unit tests for Magpie featurization"
      min_lines: 40
    - path: "tests/test_split.py"
      provides: "Unit tests for compositional splitting"
      min_lines: 50
  key_links:
    - from: "cathode_ml/features/composition.py"
      to: "matminer.featurizers.composition.ElementProperty"
      via: "from_preset('magpie') for 132-dim descriptors"
      pattern: "from_preset.*magpie"
    - from: "cathode_ml/features/split.py"
      to: "sklearn.model_selection.GroupShuffleSplit"
      via: "Group-based train/val/test splitting"
      pattern: "GroupShuffleSplit"
    - from: "cathode_ml/features/split.py"
      to: "pymatgen.core.Composition"
      via: "reduced_formula for group key normalization"
      pattern: "reduced_formula"
---

<objective>
Implement Magpie composition featurization and compositional group splitting.

Purpose: Create the tabular feature pipeline (Magpie descriptors) that feeds RF/XGBoost baselines, and the anti-leakage splitting strategy used by ALL models in this project (baselines and GNNs). These are foundational for Plan 03 (baselines) and Phases 3-4 (GNNs).
Output: composition.py, split.py, test suites for both
</objective>

<execution_context>
@C:/Users/regis/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/regis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-featurization-and-baseline-models/02-RESEARCH.md

<interfaces>
<!-- Phase 1 interfaces consumed by this plan -->

From cathode_ml/data/schemas.py:
```python
@dataclass
class MaterialRecord:
    material_id: str
    formula: str                   # Chemical formula string
    structure_dict: dict
    source: str
    formation_energy_per_atom: Optional[float] = None
    energy_above_hull: Optional[float] = None
    voltage: Optional[float] = None
    capacity: Optional[float] = None
    is_stable: Optional[bool] = None
    space_group: Optional[int] = None
```

From cathode_ml/config.py:
```python
def load_config(config_path: str = "configs/data.yaml") -> dict
def set_seeds(config: dict) -> None
```

<!-- configs/features.yaml created by Plan 01 (Wave 1 parallel) but this plan does not depend on it.
     split.py reads split config, but the function accepts params directly. Config loading is caller's job. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Magpie composition featurizer with NaN handling</name>
  <files>cathode_ml/features/composition.py, tests/test_composition.py, requirements.txt</files>
  <behavior>
    - test_magpie_features_shape: featurize_compositions(["LiCoO2", "LiFePO4"]) returns ndarray of shape (2, 132)
    - test_magpie_feature_labels: Returns list of 132 feature label strings
    - test_nan_handling: Single-element compositions (e.g., "Li") produce finite values after imputation (no NaN in output)
    - test_all_nan_column_dropped: If a feature column is entirely NaN, it is dropped and feature count < 132
    - test_empty_input: featurize_compositions([]) returns empty array with correct dimensions
  </behavior>
  <action>
    1. Create `cathode_ml/features/composition.py` with function:
       - `featurize_compositions(formulas: list[str]) -> tuple[np.ndarray, list[str]]`: Use `ElementProperty.from_preset("magpie")`. Create DataFrame with "formula" column, add "composition" column via `Composition(f)`. Call `featurizer.featurize_dataframe(df, col_id="composition", ignore_errors=True)`. Extract feature columns via `featurizer.feature_labels()`. Drop columns that are entirely NaN. Impute remaining NaN with column median. Return (X ndarray, feature_label list). Handle empty input gracefully (return shape (0, 0) array and empty list).

    2. Create `tests/test_composition.py` with tests per behavior spec. Use small formula lists for speed (LiCoO2, LiFePO4, Li for edge cases).

    3. Update `requirements.txt`: Add matminer==0.9.3 (if not present).
  </action>
  <verify>
    <automated>pytest tests/test_composition.py -x --timeout=60</automated>
  </verify>
  <done>
    - Magpie featurizer produces 132-dim vectors for standard compositions
    - NaN values are imputed with column median
    - All-NaN columns are dropped
    - Empty input handled gracefully
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compositional group splitter preventing polymorph leakage</name>
  <files>cathode_ml/features/split.py, tests/test_split.py</files>
  <behavior>
    - test_get_group_keys: get_group_keys(["Li2Co2O4", "LiCoO2", "LiFePO4"]) returns ["LiCoO2", "LiCoO2", "LiFePO4"] (normalized reduced formulas)
    - test_group_split_sizes: compositional_split with 100 samples produces ~80/10/10 train/val/test
    - test_no_formula_overlap: No reduced formula appears in both train and test index sets
    - test_no_formula_overlap_val: No reduced formula appears in both val and test index sets
    - test_deterministic_with_seed: Same seed produces identical splits across two calls
    - test_group_split_no_leakage: For synthetic data with known polymorphs (same reduced formula, different material_ids), all polymorphs end up in the same split
  </behavior>
  <action>
    1. Create `cathode_ml/features/split.py` with two functions:
       - `get_group_keys(formulas: list[str]) -> list[str]`: Normalize each formula via `Composition(f).reduced_formula`. Return list of reduced formula strings.
       - `compositional_split(n_samples: int, groups: list[str], test_size: float = 0.1, val_size: float = 0.1, seed: int = 42) -> tuple[np.ndarray, np.ndarray, np.ndarray]`: Use GroupShuffleSplit twice. First split: separate test set (test_size). Second split on trainval remainder: separate val (val_frac = val_size / (1 - test_size)). Return (train_indices, val_indices, test_indices) as numpy arrays. Use numpy array indexing for groups_trainval subset.

    2. Create `tests/test_split.py` with tests per behavior spec. Use synthetic formulas with known polymorphs (e.g., 5 entries of "LiCoO2", 5 of "LiFePO4", 5 of "LiMnO2", etc.) to verify group integrity.
  </action>
  <verify>
    <automated>pytest tests/test_split.py -x --timeout=60</automated>
  </verify>
  <done>
    - Reduced formula normalization groups polymorphs correctly
    - Train/val/test splits have no formula overlap
    - Splits are deterministic with same seed
    - Approximate 80/10/10 proportions maintained
  </done>
</task>

</tasks>

<verification>
pytest tests/test_composition.py tests/test_split.py -v --timeout=60
</verification>

<success_criteria>
- Magpie descriptors produce correct-dimensioned feature vectors for cathode formulas
- NaN handling prevents downstream ML failures
- Compositional splits prevent polymorph leakage between train/val/test
- All splits deterministic with fixed seed
</success_criteria>

<output>
After completion, create `.planning/phases/02-featurization-and-baseline-models/02-02-SUMMARY.md`
</output>
