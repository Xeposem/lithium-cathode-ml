---
phase: 11-data-validation-retraining
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - data/results/baselines/results.json
  - data/results/cgcnn/results.json
  - data/results/m3gnet/results.json
  - data/results/tensornet/results.json
  - data/results/comparison/model_comparison.csv
  - data/processed/materials.json
autonomous: false
requirements: [DATA-03]

must_haves:
  truths:
    - "Combined 4-source dataset in data/processed/materials.json is larger than the previous 2-source dataset"
    - "RF model has fresh metrics on the 4-source dataset"
    - "XGBoost model has fresh metrics on the 4-source dataset"
    - "CGCNN model has fresh metrics on the 4-source dataset"
    - "M3GNet model has fresh metrics on the 4-source dataset"
    - "TensorNet model has fresh metrics on the 4-source dataset"
    - "All model results files have timestamps newer than the pipeline run"
  artifacts:
    - path: "data/processed/materials.json"
      provides: "Combined 4-source cleaned dataset"
      min_lines: 100
    - path: "data/results/comparison/model_comparison.csv"
      provides: "Updated model comparison with all 5 models"
      contains: "cgcnn"
  key_links:
    - from: "cathode_ml/pipeline.py"
      to: "data/processed/materials.json"
      via: "run_fetch_stage writes cleaned records"
      pattern: "materials.json"
    - from: "cathode_ml/pipeline.py"
      to: "data/results/"
      via: "run_train_stage writes per-model results"
      pattern: "results"
---

<objective>
Run the full pipeline with all 4 data sources and all 5 models to produce fresh metrics on the combined dataset.

Purpose: DATA-03 requires retraining all models on the 4-source combined data and storing updated metrics. This is the culmination of the data validation phase.
Output: Fresh data/processed/materials.json with 4-source data, updated results for all 5 models, updated comparison tables.
</objective>

<execution_context>
@C:/Users/regis/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/regis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-data-validation-retraining/11-01-SUMMARY.md

@cathode_ml/pipeline.py
@configs/data.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full 4-source pipeline with all models</name>
  <files>data/processed/materials.json, data/results/baselines/results.json, data/results/cgcnn/results.json, data/results/m3gnet/results.json, data/results/tensornet/results.json, data/results/comparison/model_comparison.csv</files>
  <action>
1. First, record the current dataset size for comparison:
   ```bash
   python -c "import json; d=json.load(open('data/processed/materials.json')); print(f'Current dataset: {len(d)} records, sources: {set(r[\"source\"] for r in d)}')"
   ```

2. Run the full pipeline with all 4 sources refreshed and all 5 models:
   ```bash
   python -m cathode_ml --refresh all --models rf xgb cgcnn m3gnet tensornet
   ```
   This will:
   - Fetch from MP, OQMD, AFLOW, JARVIS (using cache except where --refresh forces re-fetch)
   - Clean and merge all records
   - Save to data/processed/materials.json
   - Train RF, XGBoost, CGCNN, M3GNet, TensorNet on the combined dataset
   - Run evaluation to generate comparison tables and plots

   NOTE: This will take significant time (30-60+ minutes depending on hardware). The GNN models (CGCNN, M3GNet, TensorNet) are the bottleneck. Monitor for errors in the log output.

   If any model fails to train, investigate and fix. Common issues:
   - DGL CUDA detection (already fixed in v1.0) -- ensure CPU fallback works
   - Memory issues with large dataset -- may need to adjust batch sizes in configs
   - AFLOW/JARVIS structures that fail graph conversion -- the cleaning pipeline should have filtered these

3. After pipeline completes, verify the new dataset size:
   ```bash
   python -c "import json; d=json.load(open('data/processed/materials.json')); print(f'New dataset: {len(d)} records'); sources={r['source'] for r in d}; print(f'Sources: {sources}'); assert len(sources) >= 3, f'Expected 3+ sources, got {sources}'"
   ```
   The combined dataset MUST contain records from at least 3 sources (ideally 4). It must be larger than the previous 2-source dataset.

4. Verify results exist for all 5 models:
   ```bash
   for model in baselines cgcnn m3gnet tensornet; do
     echo "=== $model ==="; ls -la data/results/$model/ | head -5
   done
   ```
  </action>
  <verify>
    <automated>cd D:/GitRepo/lithium-cathode-ml && python -c "
import json, os
# Check dataset has 3+ sources
d = json.load(open('data/processed/materials.json'))
sources = {r['source'] for r in d}
print(f'Dataset: {len(d)} records from {sources}')
assert len(sources) >= 3, f'Need 3+ sources, got {sources}'
assert len(d) > 100, f'Dataset too small: {len(d)}'
# Check all model results exist
for model_dir in ['baselines', 'cgcnn', 'm3gnet', 'tensornet']:
    path = f'data/results/{model_dir}'
    assert os.path.isdir(path), f'Missing {path}'
    files = os.listdir(path)
    assert len(files) > 0, f'{path} is empty'
    print(f'{model_dir}: {len(files)} files')
# Check comparison table
assert os.path.exists('data/results/comparison/model_comparison.csv'), 'Missing comparison CSV'
print('All checks passed')
"</automated>
  </verify>
  <done>4-source dataset created with more records than the previous 2-source dataset. All 5 models trained successfully. Results and comparison tables updated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify retrained model metrics are reasonable</name>
  <files>data/results/comparison/model_comparison.csv</files>
  <action>Present the model comparison results to the user for review. Display the comparison CSV and highlight key metrics (R-squared, MAE) for all 5 models on formation energy.</action>
  <verify>User confirms metrics are reasonable</verify>
  <done>User has approved the 4-source retrained model metrics as reasonable</done>
  <what-built>Full 4-source pipeline run with all 5 models retrained on the combined dataset. Fresh metrics stored in data/results/.</what-built>
  <how-to-verify>
    1. Check the pipeline output log for any errors or warnings
    2. Review data/results/comparison/model_comparison.csv -- all 5 models should have R-squared and MAE values
    3. Verify the dataset size increased: `python -c "import json; d=json.load(open('data/processed/materials.json')); print(len(d), 'records from', {r['source'] for r in d})"`
    4. Spot-check a few model results look reasonable (positive R-squared for formation energy)
    5. Optionally run: `python -m cathode_ml --stage evaluate` to regenerate comparison plots
  </how-to-verify>
  <resume-signal>Type "approved" if metrics look reasonable, or describe any issues with model performance</resume-signal>
</task>

</tasks>

<verification>
- data/processed/materials.json contains records from 3+ sources (ideally 4: mp, oqmd, aflow, jarvis)
- data/results/{baselines,cgcnn,m3gnet,tensornet}/ all contain fresh result files
- data/results/comparison/model_comparison.csv exists with all 5 models
- Dataset size is larger than the previous 2-source dataset
</verification>

<success_criteria>
- Combined dataset has records from at least 3 data sources (DATA-03)
- All 5 models (RF, XGBoost, CGCNN, M3GNet, TensorNet) have fresh metrics (DATA-03)
- Model comparison table reflects 4-source training results
- User has approved the metrics as reasonable
</success_criteria>

<output>
After completion, create `.planning/phases/11-data-validation-retraining/11-02-SUMMARY.md`
</output>
