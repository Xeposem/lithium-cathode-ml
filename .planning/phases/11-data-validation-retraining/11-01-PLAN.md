---
phase: 11-data-validation-retraining
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cathode_ml/pipeline.py
  - tests/test_data_validation.py
autonomous: true
requirements: [DATA-01, DATA-02]

must_haves:
  truths:
    - "AFLOW fetcher returns MaterialRecord objects with valid structure_dict fields"
    - "JARVIS fetcher returns MaterialRecord objects with valid structure_dict fields"
    - "AFLOW records pass through CleaningPipeline without errors"
    - "JARVIS records pass through CleaningPipeline without errors"
    - "AFLOW records convert to PyG graph Data objects without errors"
    - "JARVIS records convert to PyG graph Data objects without errors"
    - "pipeline.py refresh-all expands to all 4 sources (not stale bdg)"
  artifacts:
    - path: "tests/test_data_validation.py"
      provides: "End-to-end integration tests for AFLOW and JARVIS data paths"
      min_lines: 80
    - path: "cathode_ml/pipeline.py"
      provides: "Fixed refresh source expansion"
      contains: "aflow"
  key_links:
    - from: "cathode_ml/data/aflow_fetcher.py"
      to: "cathode_ml/data/clean.py"
      via: "MaterialRecord objects passed to CleaningPipeline.run()"
      pattern: "CleaningPipeline.*run"
    - from: "cathode_ml/data/jarvis_fetcher.py"
      to: "cathode_ml/data/clean.py"
      via: "MaterialRecord objects passed to CleaningPipeline.run()"
      pattern: "CleaningPipeline.*run"
    - from: "cathode_ml/data/clean.py"
      to: "cathode_ml/features/graph.py"
      via: "cleaned records structure_dict -> Structure -> structure_to_graph"
      pattern: "structure_to_graph"
---

<objective>
Validate that AFLOW and JARVIS fetchers produce data that flows end-to-end through the cleaning pipeline and graph conversion without errors, and fix the stale "bdg" reference in pipeline.py.

Purpose: DATA-01 and DATA-02 require proving the new data sources work through the full pipeline. A bug in pipeline.py also prevents --refresh all from refreshing AFLOW/JARVIS.
Output: Integration tests proving end-to-end flow, fixed pipeline.py
</objective>

<execution_context>
@C:/Users/regis/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/regis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-model-bug-fixes/10-01-SUMMARY.md

@cathode_ml/data/aflow_fetcher.py
@cathode_ml/data/jarvis_fetcher.py
@cathode_ml/data/fetch.py
@cathode_ml/data/clean.py
@cathode_ml/data/schemas.py
@cathode_ml/features/graph.py
@cathode_ml/pipeline.py
@configs/data.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix pipeline.py refresh bug and create end-to-end validation tests</name>
  <files>cathode_ml/pipeline.py, tests/test_data_validation.py</files>
  <action>
1. Fix `cathode_ml/pipeline.py` line 119 in `run_fetch_stage()`: change
   `refresh = {"mp", "oqmd", "bdg"}` to `refresh = {"mp", "oqmd", "aflow", "jarvis"}`
   This is a leftover from the BDG removal in v1.0.

2. Create `tests/test_data_validation.py` with integration tests that validate the AFLOW and JARVIS data paths end-to-end. Tests should use synthetic MaterialRecord fixtures (not live API calls) to prove:

   a. **AFLOW path tests:**
   - Create 3-5 synthetic MaterialRecord objects with source="aflow", valid structure_dict (use a simple Li2O-like pymatgen Structure.as_dict()), valid formation_energy_per_atom values in range [-3.0, 0.0].
   - Pass them through CleaningPipeline.run() with the standard config from configs/data.yaml -- assert no errors and at least 1 record survives cleaning.
   - Take a surviving cleaned record, convert structure_dict to pymatgen Structure, call structure_to_graph() with standard graph config -- assert it returns a torch_geometric Data object with node features (x), edge_index, and edge_attr.

   b. **JARVIS path tests:**
   - Same as above but with source="jarvis" records.

   c. **Mixed 4-source test:**
   - Create records from all 4 sources (mp, oqmd, aflow, jarvis), run through CleaningPipeline, assert records from all 4 sources can survive (not all filtered out by source-specific issues).

   d. **Pipeline refresh fix test:**
   - Assert that `run_fetch_stage` with args.refresh=["all"] would expand to include "aflow" and "jarvis" (can test by importing and checking the expansion logic, or by mocking run_fetch and inspecting the refresh_sources argument).

   Use pymatgen to create valid test structures:
   ```python
   from pymatgen.core import Lattice, Structure
   lattice = Lattice.cubic(4.2)
   structure = Structure(lattice, ["Li", "Li", "O"], [[0,0,0], [0.5,0.5,0], [0.5,0,0.5]])
   structure_dict = structure.as_dict()
   ```

   Load graph config from configs/features.yaml for structure_to_graph calls.
  </action>
  <verify>
    <automated>cd D:/GitRepo/lithium-cathode-ml && python -m pytest tests/test_data_validation.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <done>All tests pass: AFLOW records flow through clean+graph, JARVIS records flow through clean+graph, mixed 4-source records survive cleaning, pipeline.py refresh expands correctly</done>
</task>

<task type="auto">
  <name>Task 2: Run live fetch validation for AFLOW and JARVIS</name>
  <files>tests/test_data_validation.py</files>
  <action>
Add a separate test class `TestLiveFetchValidation` (marked with `@pytest.mark.slow` so it is skipped by default) that does actual fetching with small limits:

1. **AFLOW live test:**
   - Instantiate AFLOWFetcher with a config limiting max_entries to 10.
   - Call fetch(), assert it returns a non-empty list of MaterialRecord.
   - Check that at least one record has a non-empty structure_dict.
   - Pass records through CleaningPipeline with standard config filters.
   - Convert at least one cleaned record to a graph.

2. **JARVIS live test:**
   - Instantiate JARVISFetcher with standard config.
   - Call fetch() (JARVIS downloads the full dft_3d dataset then filters; this is slow but necessary for validation).
   - Assert it returns a non-empty list of MaterialRecord.
   - Check that at least one record has a non-empty structure_dict.
   - Pass records through CleaningPipeline.
   - Convert at least one cleaned record to a graph.

These tests prove the actual API/data format hasn't changed. Mark with `@pytest.mark.slow` decorator. Then run the slow tests to validate.

After running slow tests, print a summary: number of AFLOW records fetched, number surviving cleaning, number successfully converted to graphs. Same for JARVIS.
  </action>
  <verify>
    <automated>cd D:/GitRepo/lithium-cathode-ml && python -m pytest tests/test_data_validation.py::TestLiveFetchValidation -x -v --timeout=600 -m slow 2>&1 | tail -40</automated>
  </verify>
  <done>Live AFLOW and JARVIS fetches succeed, records pass through cleaning and graph conversion. At least 1 record from each source converts to a valid PyG graph.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_data_validation.py -x -v` -- all synthetic tests pass
- `python -m pytest tests/test_data_validation.py -x -v -m slow` -- live fetch tests pass
- `grep "aflow.*jarvis" cathode_ml/pipeline.py` confirms refresh fix
</verification>

<success_criteria>
- AFLOW records survive fetch -> clean -> graph conversion (DATA-01)
- JARVIS records survive fetch -> clean -> graph conversion (DATA-02)
- pipeline.py --refresh all correctly expands to include aflow and jarvis
- Both synthetic and live validation tests exist and pass
</success_criteria>

<output>
After completion, create `.planning/phases/11-data-validation-retraining/11-01-SUMMARY.md`
</output>
