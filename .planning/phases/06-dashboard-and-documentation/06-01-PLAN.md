---
phase: 06-dashboard-and-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/app.py
  - dashboard/pages/overview.py
  - dashboard/pages/model_comparison.py
  - dashboard/utils/__init__.py
  - dashboard/utils/data_loader.py
  - dashboard/utils/charts.py
  - tests/test_dashboard.py
  - requirements.txt
autonomous: true
requirements: [DASH-01, DASH-04]

must_haves:
  truths:
    - "Dashboard launches with `streamlit run dashboard/app.py` and shows sidebar navigation"
    - "Overview page displays model comparison metrics table and MAE bar chart"
    - "Model Comparison page shows per-property metrics and training curves"
    - "All charts use Wong colorblind-safe palette consistent with evaluation module"
  artifacts:
    - path: "dashboard/app.py"
      provides: "Streamlit entrypoint with st.Page/st.navigation"
      contains: "st.navigation"
    - path: "dashboard/utils/data_loader.py"
      provides: "Cached data loading functions wrapping evaluation/metrics.py"
      contains: "st.cache_data"
    - path: "dashboard/utils/charts.py"
      provides: "Plotly chart factory with consistent MODEL_COLORS styling"
      contains: "MODEL_COLORS"
    - path: "dashboard/pages/overview.py"
      provides: "Landing page with metrics table and bar chart"
    - path: "dashboard/pages/model_comparison.py"
      provides: "Per-property comparison and training curves"
    - path: "tests/test_dashboard.py"
      provides: "Unit tests for dashboard utility functions"
  key_links:
    - from: "dashboard/utils/data_loader.py"
      to: "cathode_ml/evaluation/metrics.py"
      via: "import load_all_results, MODEL_COLORS, MODELS_ORDER, PROPERTIES"
      pattern: "from cathode_ml\\.evaluation\\.metrics import"
    - from: "dashboard/utils/charts.py"
      to: "cathode_ml/evaluation/metrics.py"
      via: "MODEL_COLORS, MODEL_LABELS for consistent Plotly styling"
      pattern: "MODEL_COLORS"
    - from: "dashboard/pages/overview.py"
      to: "dashboard/utils/data_loader.py"
      via: "get_all_results() call"
      pattern: "get_all_results"
---

<objective>
Create the Streamlit dashboard foundation with app entrypoint, shared utility layer (data loading, chart factories), test scaffold, and the two results-oriented pages (Overview and Model Comparison).

Purpose: Establishes the reusable dashboard infrastructure that all other pages depend on, and delivers the metrics-first landing experience (DASH-01) plus training curve display (DASH-04).

Output: Runnable multi-page Streamlit app with Overview and Model Comparison pages, plus unit-testable utility modules.
</objective>

<execution_context>
@C:/Users/regis/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/regis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-dashboard-and-documentation/06-CONTEXT.md
@.planning/phases/06-dashboard-and-documentation/06-RESEARCH.md
@.planning/phases/06-dashboard-and-documentation/06-VALIDATION.md

<interfaces>
<!-- Key types and contracts from existing codebase -->

From cathode_ml/evaluation/metrics.py:
```python
MODEL_COLORS: dict[str, str] = {
    "rf": "#0072B2", "xgb": "#D55E00", "cgcnn": "#009E73", "megnet": "#CC79A7",
}
MODEL_LABELS: dict[str, str] = {
    "rf": "RF", "xgb": "XGBoost", "cgcnn": "CGCNN", "megnet": "MEGNet†",
}
MODELS_ORDER: list[str] = ["rf", "xgb", "cgcnn", "megnet"]
PROPERTIES: list[str] = [
    "formation_energy_per_atom", "voltage", "capacity", "energy_above_hull",
]
def load_all_results(results_base: str = "data/results") -> dict:
    """Returns {property: {model: {mae, rmse, r2, n_train, n_test}}}"""
```

From cathode_ml/data/cache.py:
```python
class DataCache:
    def __init__(self, cache_dir: str) -> None: ...
    def load(self, key: str) -> Any: ...  # Returns data field only
    def has(self, key: str) -> bool: ...
```

Training CSV format (data/results/{model}/{property}_metrics.csv):
Columns: epoch, train_loss, val_loss, val_mae, lr
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dashboard utility layer and test scaffold</name>
  <files>dashboard/__init__.py, dashboard/utils/__init__.py, dashboard/utils/data_loader.py, dashboard/utils/charts.py, tests/test_dashboard.py, requirements.txt</files>
  <behavior>
    - test_get_all_results: get_all_results() returns dict with expected structure when results exist
    - test_get_all_results_empty: returns empty dict when no results dir
    - test_get_cached_records: get_cached_records() returns list of dicts
    - test_make_bar_comparison: returns Plotly Figure with correct number of traces matching available models
    - test_make_bar_comparison_colors: bar colors match MODEL_COLORS values
    - test_make_training_curves: returns Plotly Figure with train/val traces from CSV data
    - test_make_training_curves_missing: returns None when CSV not found
    - test_make_parity_plot: returns Plotly Figure with scatter trace and diagonal line
  </behavior>
  <action>
  1. Add `streamlit>=1.55.0`, `plotly>=5.18.0`, `stmol>=0.0.9`, `py3Dmol>=2.1.0` to requirements.txt (append to existing, do not remove existing deps).

  2. Create `dashboard/__init__.py` (empty) and `dashboard/utils/__init__.py` (empty).

  3. Create `dashboard/utils/data_loader.py`:
     - `get_all_results(results_base="data/results") -> dict`: wraps `load_all_results()` with `@st.cache_data`
     - `get_cached_records(cache_dir="data/cache") -> list[dict]`: loads `DataCache(cache_dir).load("cleaned_records")` with `@st.cache_data`, returns list of dicts (converts MaterialRecord dataclasses via `dataclasses.asdict` if needed)
     - `get_training_csv(results_base, model, prop) -> pd.DataFrame | None`: reads CSV from `{results_base}/{model}/{prop}_metrics.csv`, returns None if missing. Cached with `@st.cache_data`.
     - Import `PROPERTIES`, `MODELS_ORDER`, `MODEL_COLORS`, `MODEL_LABELS` from metrics.py and re-export them for convenience.

  4. Create `dashboard/utils/charts.py`:
     - `make_bar_comparison(all_results: dict, properties: list[str]) -> go.Figure`: Grouped bar chart of MAE per model per property. Uses MODEL_COLORS, MODEL_LABELS, MODELS_ORDER. Layout: barmode="group", yaxis_title="MAE", legend at top.
     - `make_training_curves(df: pd.DataFrame, model: str, prop: str) -> go.Figure`: Line chart from training CSV DataFrame. Train loss (#333333) and val loss (MODEL_COLORS[model]) vs epoch. Title: "{MODEL_LABELS[model]} - {prop title}".
     - `make_parity_plot(y_true, y_pred, model: str, prop: str, ids=None) -> go.Figure`: Interactive scatter with hover showing material ID (if provided). Diagonal reference line. Color from MODEL_COLORS. Title: "{MODEL_LABELS[model]} - {prop title}".
     - All figures use `width="stretch"` compatible layout (no explicit width, let Streamlit handle it).

  5. Create `tests/test_dashboard.py` with the tests described in behavior block. Mock file I/O and st.cache decorators. Tests should be importable without streamlit running (mock `st.cache_data` as identity decorator).

  NOTE: Use `width="stretch"` (NOT `use_container_width=True`) per Streamlit late-2025 deprecation.
  </action>
  <verify>
    <automated>python -m pytest tests/test_dashboard.py -x -v</automated>
  </verify>
  <done>All 8+ tests pass. data_loader.py and charts.py importable and functional. requirements.txt updated with new deps.</done>
</task>

<task type="auto">
  <name>Task 2: App entrypoint, Overview page, and Model Comparison page</name>
  <files>dashboard/app.py, dashboard/pages/overview.py, dashboard/pages/model_comparison.py</files>
  <action>
  1. Create `dashboard/app.py` using st.Page + st.navigation API:
     - `st.set_page_config(page_title="Cathode ML Dashboard", page_icon=":material/battery_charging_full:", layout="wide")`
     - Define all 6 pages with st.Page pointing to pages/ files (Overview, Data Explorer, Model Comparison, Materials Explorer, Predict, Crystal Viewer). Group into sections: "Results" (Overview, Model Comparison), "Explore" (Data Explorer, Materials Explorer), "Tools" (Predict, Crystal Viewer).
     - Pages not yet implemented should have placeholder files that show `st.info("Coming soon")`.
     - Run `pg.run()`.

  2. Create placeholder page files for pages not built in this plan:
     - `dashboard/pages/data_explorer.py`: `st.title("Data Explorer"); st.info("Coming in Plan 03")`
     - `dashboard/pages/materials_explorer.py`: same pattern
     - `dashboard/pages/predict.py`: same pattern
     - `dashboard/pages/crystal_viewer.py`: same pattern

  3. Create `dashboard/pages/overview.py` (DASH-01 landing page):
     - Title: "Cathode ML: Model Performance Overview"
     - Load results via `get_all_results()` from utils.data_loader
     - Display a key findings summary in `st.markdown()`: brief paragraph about the project (lithium cathode property prediction, 4 models compared)
     - Show metrics summary table using `st.dataframe()` with columns: Property, Best Model, MAE, R-squared. One row per property showing the best-performing model.
     - Show MAE bar comparison chart via `make_bar_comparison()` rendered with `st.plotly_chart(fig, width="stretch")`
     - Handle empty results gracefully with `st.warning("No results found")`

  4. Create `dashboard/pages/model_comparison.py` (DASH-01 detailed + DASH-04):
     - Title: "Model Comparison"
     - Property selector: `st.selectbox("Property", PROPERTIES)`
     - For selected property: show full metrics table (all models, MAE/RMSE/R2) via `st.dataframe()`
     - Parity plot section: placeholder text noting "Parity plots require prediction arrays (available after full pipeline run)"
     - Training curves section (DASH-04): For each GNN model (cgcnn, megnet), load CSV via `get_training_csv()` and render via `make_training_curves()`. Show curves side-by-side using `st.columns(2)`. Skip models without training CSVs.
     - Baselines don't have per-epoch training data, so note "Baselines (RF, XGBoost) do not have per-epoch training curves" in an `st.caption()`.
  </action>
  <verify>
    <automated>python -c "import dashboard.app; import dashboard.pages.overview; import dashboard.pages.model_comparison; print('All dashboard modules importable')"</automated>
  </verify>
  <done>Dashboard launches with `streamlit run dashboard/app.py`. Overview shows metrics table and bar chart. Model Comparison shows per-property metrics and training curves for GNNs. Sidebar navigation works across all 6 page entries (4 show placeholders).</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_dashboard.py -x -v` -- all utility tests pass
2. `python -c "import dashboard.app"` -- app module importable without errors
3. All dashboard pages importable without Streamlit server running
</verification>

<success_criteria>
- `streamlit run dashboard/app.py` launches multi-page app with sidebar
- Overview page shows metrics table + bar chart with correct MODEL_COLORS
- Model Comparison page shows per-property metrics + training curves
- 4 placeholder pages render "Coming soon" messages
- All unit tests in test_dashboard.py pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-dashboard-and-documentation/06-01-SUMMARY.md`
</output>
