---
phase: 06-dashboard-and-documentation
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - dashboard/pages/data_explorer.py
  - dashboard/pages/materials_explorer.py
  - tests/test_dashboard.py
autonomous: true
requirements: [DASH-03, DASH-05, DASH-06]

must_haves:
  truths:
    - "User can browse dataset distributions via histograms for each property"
    - "User can explore property correlations via interactive scatter matrix"
    - "User can filter materials by voltage, formation energy, capacity, elements, and stability"
    - "Discovery panel shows top candidate materials ranked by predicted properties"
  artifacts:
    - path: "dashboard/pages/data_explorer.py"
      provides: "Dataset browser with histograms and scatter matrix"
      contains: "histogram"
    - path: "dashboard/pages/materials_explorer.py"
      provides: "Filterable materials table with discovery panel"
      contains: "multiselect"
  key_links:
    - from: "dashboard/pages/data_explorer.py"
      to: "dashboard/utils/data_loader.py"
      via: "get_cached_records() for loading cleaned dataset"
      pattern: "get_cached_records"
    - from: "dashboard/pages/materials_explorer.py"
      to: "dashboard/utils/data_loader.py"
      via: "get_cached_records() for materials data"
      pattern: "get_cached_records"
---

<objective>
Build the Data Explorer page (dataset distributions and correlations) and Materials Explorer page (filterable materials table with discovery panel).

Purpose: Enables dataset exploration (DASH-03) and materials discovery (DASH-05, DASH-06), completing the "Explore" section of the dashboard.

Output: Two fully functional explorer pages with interactive filtering and visualization.
</objective>

<execution_context>
@C:/Users/regis/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/regis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-dashboard-and-documentation/06-CONTEXT.md
@.planning/phases/06-dashboard-and-documentation/06-RESEARCH.md
@.planning/phases/06-dashboard-and-documentation/06-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 utilities -->

From dashboard/utils/data_loader.py (created in Plan 01):
```python
@st.cache_data
def get_all_results(results_base="data/results") -> dict:
    """Returns {property: {model: {mae, rmse, r2, n_train, n_test}}}"""

@st.cache_data
def get_cached_records(cache_dir="data/cache") -> list[dict]:
    """Returns list of material record dicts with keys:
    material_id, formula, structure_dict, source,
    formation_energy_per_atom, energy_above_hull, voltage, capacity,
    is_stable, space_group"""

# Re-exported from cathode_ml.evaluation.metrics:
PROPERTIES, MODELS_ORDER, MODEL_COLORS, MODEL_LABELS
```

From cathode_ml/data/schemas.py:
```python
@dataclass
class MaterialRecord:
    material_id: str; formula: str; structure_dict: dict; source: str
    formation_energy_per_atom: Optional[float]; energy_above_hull: Optional[float]
    voltage: Optional[float]; capacity: Optional[float]
    is_stable: Optional[bool]; space_group: Optional[int]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Data Explorer page with histograms and scatter matrix</name>
  <files>dashboard/pages/data_explorer.py, tests/test_dashboard.py</files>
  <behavior>
    - test_data_explorer_load: get_cached_records returns non-empty list (integration-style with mock cache)
    - test_histogram_creation: histogram function returns Plotly Figure with expected axis labels
    - test_scatter_matrix_creation: scatter matrix function returns Plotly Figure
  </behavior>
  <action>
  1. Add helper functions to `dashboard/pages/data_explorer.py` (or inline, keeping page self-contained):
     - `_make_histogram(df, column, color) -> go.Figure`: Plotly histogram for a single property column. Title: property name (human-readable). Color: "#0072B2" (neutral blue). Hover shows count.
     - `_make_scatter_matrix(df, columns) -> go.Figure`: Plotly scatter_matrix (px.scatter_matrix) showing pairwise correlations. Color by source if available. Diagonal shows histograms.

  2. Create `dashboard/pages/data_explorer.py` (DASH-03):
     - Title: "Data Explorer"
     - Load records via `get_cached_records()`, convert to pandas DataFrame.
     - Show dataset summary: `st.metric()` cards for total records, records per source (MP, OQMD, BDG), properties available.
     - Property histograms section: For each property in PROPERTIES, show a histogram. Use `st.columns(2)` to display 2 per row. Only include properties that have non-null values. Human-readable labels: "Formation Energy (eV/atom)", "Voltage (V)", "Capacity (mAh/g)", "Energy Above Hull (eV/atom)".
     - Scatter matrix section: `st.subheader("Property Correlations")`. Create scatter matrix of numeric properties. Add a multiselect to choose which properties to include (default: all).
     - Handle empty dataset with `st.warning("No cached data found")`.

  3. Add tests to `tests/test_dashboard.py` (append to existing file from Plan 01):
     - test_data_explorer_load: mock DataCache to return sample records, verify get_cached_records returns data.
     - test_histogram_creation: call histogram helper with sample DataFrame, verify returns go.Figure.
     - test_scatter_matrix_creation: call scatter matrix helper, verify returns go.Figure.
  </action>
  <verify>
    <automated>python -m pytest tests/test_dashboard.py::test_data_explorer_load tests/test_dashboard.py::test_histogram_creation tests/test_dashboard.py::test_scatter_matrix_creation -x -v</automated>
  </verify>
  <done>Data Explorer page shows property histograms (2 per row) and interactive scatter matrix. Dataset summary metrics displayed at top. All 3 new tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Materials Explorer page with filtering and discovery panel</name>
  <files>dashboard/pages/materials_explorer.py, tests/test_dashboard.py</files>
  <behavior>
    - test_materials_filter_voltage: filtering by voltage range returns subset of records
    - test_materials_filter_elements: filtering by elements returns only matching formulas
    - test_materials_filter_stability: stable-only filter excludes unstable materials
    - test_discovery_ranking: ranking by voltage returns materials sorted by voltage descending
  </behavior>
  <action>
  1. Create `dashboard/pages/materials_explorer.py` (DASH-05 + DASH-06):
     - Title: "Materials Explorer"
     - Load records via `get_cached_records()`, convert to DataFrame.

     **Filterable Table Section (DASH-05):**
     - Use `st.sidebar` or `st.expander("Filters")` for filter controls:
       - Voltage range: `st.slider("Voltage (V)", min, max, (min, max))` -- only if voltage column has data
       - Formation energy range: `st.slider("Formation Energy (eV/atom)", min, max, (min, max))`
       - Capacity range: `st.slider("Capacity (mAh/g)", min, max, (min, max))`
       - Energy above hull range: `st.slider("Energy Above Hull (eV/atom)", min, max, (min, max))`
       - Element filter: `st.multiselect("Must contain elements", sorted_elements)` -- extract unique elements from formulas using regex `[A-Z][a-z]?`
       - Stability filter: `st.checkbox("Stable only (E_hull = 0)")`
       - Source filter: `st.multiselect("Data source", ["materials_project", "oqmd", "battery_data_genome"])`
     - Apply all filters to DataFrame, show count: `st.caption(f"Showing {len(filtered)} of {len(df)} materials")`
     - Display filtered table with `st.dataframe(filtered, use_container_width=True)` -- show columns: material_id, formula, source, voltage, capacity, formation_energy_per_atom, energy_above_hull, is_stable. Enable sorting.
     - NOTE: `st.dataframe` still uses `use_container_width` (not deprecated for dataframe, only for plotly_chart).

     **Discovery Panel Section (DASH-06):**
     - `st.subheader("Top Candidates")`
     - Ranking criterion selector: `st.selectbox("Rank by", ["voltage", "capacity", "formation_energy_per_atom"])` with explanation that lower formation energy = more stable.
     - Top-N slider: `st.slider("Show top N", 5, 50, 10)`
     - Sort filtered DataFrame by selected criterion (ascending for formation_energy, descending for voltage/capacity).
     - Display top-N as highlighted table with `st.dataframe()`.
     - Add `st.info()` note explaining the ranking.

  2. Extract `_extract_elements(formula: str) -> set[str]` helper using `re.findall(r'[A-Z][a-z]?', formula)`.

  3. Extract filter logic into a testable function `filter_materials(df, voltage_range, energy_range, capacity_range, hull_range, elements, stable_only, sources) -> pd.DataFrame` that can be unit tested without Streamlit.

  4. Add tests to `tests/test_dashboard.py`:
     - test_materials_filter_voltage: create sample DataFrame, apply voltage filter, verify subset
     - test_materials_filter_elements: filter by ["Li", "Fe"], verify only matching formulas
     - test_materials_filter_stability: filter stable only, verify energy_above_hull <= 0
     - test_discovery_ranking: rank by voltage desc, verify first element has highest voltage
  </action>
  <verify>
    <automated>python -m pytest tests/test_dashboard.py::test_materials_filter_voltage tests/test_dashboard.py::test_materials_filter_elements tests/test_dashboard.py::test_materials_filter_stability tests/test_dashboard.py::test_discovery_ranking -x -v</automated>
  </verify>
  <done>Materials Explorer shows filterable table with sliders/multiselect for all properties and elements. Discovery panel ranks materials by selected criterion and shows top-N. All 4 new tests pass.</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_dashboard.py -x -v` -- all dashboard tests pass (Plan 01 + Plan 03 tests)
2. Data Explorer page shows histograms and scatter matrix
3. Materials Explorer filters work correctly and discovery panel ranks materials
</verification>

<success_criteria>
- Data Explorer shows per-property histograms and interactive scatter matrix
- Materials Explorer has working filters for voltage, energy, capacity, hull, elements, stability, source
- Discovery panel shows top-N materials ranked by selected property
- All unit tests pass including new filter and chart tests
</success_criteria>

<output>
After completion, create `.planning/phases/06-dashboard-and-documentation/06-03-SUMMARY.md`
</output>
